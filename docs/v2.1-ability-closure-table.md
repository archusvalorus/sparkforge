# v2.1 Abilities — Closure Table

*Scope note (Brandon, Sep 15): the Neutral rework is POSTPONED to v2.2+ and appears nowhere below.*

*The one place for everything the Sep 15 decision packet
(`docs/v2.1-ability-decisions-handoff.md`) left open, with Claude's
recommended initial value and the reason. Answer by row ID ("CL-3: yes",
"CL-7: 12%"). "rec" = take the recommendation. Rows marked ⛔ block a
build unit (named); everything else can be tuned after the unit ships.
Runtime facts were verified against source on Sep 15.*

**Closure pass 1 (Brandon × Lyra, Sep 15): CL-1 ✅, CL-3 ✅ (precise
wiring), CL-5 design amendment ✅ (runtime classification returned below),
CL-6 verified. Everything else is PENDING — an unapproved recommendation is
NOT permission to ship it by default.** Kind: **ARCH** = architecture
blocker for a unit; **PARAM** = a number that needs an approved initial
setting before its mechanic is tested (the mechanic can be built behind
it, but it does not ship at the recommended value without a yes).

## A. Design confirmations

| ID | Kind | Item | Recommendation | Why | Status |
|---|---|---|---|---|---|
| CL-1 | ARCH | **Bloodthirsty tick model** (Option B: 50% apply on primary hits; 10% ATK per 0.5s for 3s; refresh resets duration, never stacks or delays the next tick) | Option B | Reads as "1 HP per tick" on day one and scales forever. Correction from the review: 20% ATK/s while active is the tick rate, not automatically +20% total DPS. | ✅ **APPROVED** (A4 still waits on A0) |
| CL-2 | PARAM | Overload adjuncts | ✅ **APPROVED (Brandon, Sep 17):** 3s of PER-TARGET stun immunity after a stun ends. Boss-class stun durations: **0.25s** normally, **0.5s** with enhanced (linked) Overload — that boss reduction is applied ONCE, never again through another scaling layer. | Without immunity a 35%/2s stun at 5-target chains re-stuns in a loop. | ✅ approved |
| CL-3 | ARCH | **Frost Touch T3 fragment sources.** Runtime: the ×5 *Shatter* synergy is an instant execute — NO fragments. Real fragments = Polar Vortex Iceburst shards (T1) and the T4 icicle's impact shards, both fired with `allowModifiers: false`. | Bind T3 to Iceburst + icicle shards only; Shatter stays an execute | The two shard sources exist and need a modifier opt-in. | ✅ **APPROVED — precise wiring:** opt those shards into Frost Touch SPECIFICALLY (a `appliesFrostTouch` flag on the shard spawn), NOT a blanket `allowModifiers: true`. Copy: "Iceburst shards and icicle fragments apply Frost Touch on hit." |
| CL-4 | PARAM | Returned Void projectiles (Q-V4 working rec): from the well toward nearest valid enemy, player-owned, Void damage + penetration + Anomaly apply; NO primary-shot-counter advance, NO recursive spawns, NO friendly re-absorption | Approve as the enumerated list | Minimum set that makes the return feel Void without loops. Hostile-only absorption is already approved (Q-V4). | ⏳ pending (A6 Listlessness tier) |
| CL-5 | ARCH | **Damage pipeline order** | See §D below — classification + exact formula | — | ✅ **CLOSED (Brandon, Sep 15): kaiju stays INSIDE the 90% ceiling, no exception.** Unyielding and kaiju are both percentage reductions in the shared layer; kaiju keeps its full 85% alone, stacking tops out at 90%. A0 is unblocked. |
| CL-6 | — | Overcharge | ✅ verified: +5%/s, cap +50%, reset on hit | — | ✅ closed |

## B. Numbers still blank (recommended initial values) — PENDING unless marked ✅, all PARAM

*Build the mechanic; ship it only at an approved value. None of these were approved in closure pass 1. **CL-16 approved Sep 17** (A1 Fire); **CL-7 + CL-11 approved Sep 17** (A2 Chill); **CL-2, CL-8, CL-12, CL-13 approved Sep 17** (A3 Shock); **CL-17 + CL-18 approved Sep 21** (A4a bosses take DoTs — rows placed above CL-16). Also ruled Sep 21 for A4a: **Bloodthirsty lands IN A4a** as the Bleed signature (Needlepoint retired — base crit is 0%, so without it nothing would bleed at the gate), and **the Tesla/Burn timer bug is fixed in A4a** (Tesla keeps its own 0.5s timer; it can still hold dormant stacks at its own DPS, but never prolongs Kindle's Burn). **Gate rulings (Sep 21):** "killed a bleeding enemy" = bleeding BEFORE the killing damage, for every bleeding-conditioned reward (Glass Blood still needs Bleed damage to deliver the kill); the Polar Vortex icicle inherits primary-hit eligibility (its shards do not); Burn pips are lit only while they deal active damage; a boss death earns its rewards through one chokepoint (built as A4b's opener).*

| ID | Blank | Recommendation | Why |
|---|---|---|---|
| CL-7 | Whiteout T3 elite melt damage | ✅ **APPROVED (Brandon, Sep 17): 20% of the elite's max HP, once, when damage melts the snowman.** The melt damage is ADDITIONAL to the triggering hit; the transformation is CONSUMED BEFORE the extra damage applies, so it can never trigger twice. The elite may die from either damage event ("no execution" ≠ guaranteed survival). Transformation ends; bosses stay immune. **T3 copy:** "Damaging a snowman melts it. Normal enemies die instantly; elites take an additional 20% of max HP as damage. Bosses cannot become snowmen." | Matches Glacial Spikes' and Anomaly's elite number — one "elite chunk" constant across the trees. |
| CL-8 | Skybeam T5 splash | ✅ **APPROVED (Brandon, Sep 17):** strike every 5s, preferring a valid lassoed target, otherwise the nearest valid enemy. Primary takes **300% ATK**; each secondary within **80pt** takes **150% ATK**. The primary never receives its own splash. | The banked rework's numbers; the `updateSkybeam` early-return trap becomes "pick nearest instead." |
| CL-9 | Singularity (×7) decompose | **6% max HP/s to trapped normals; elites 2%/s capped at 40% total per trap; bosses immune to trap, take 1%/s while overlapping** | Bounded like Anomaly; a boss must never be stuck. |
| CL-10 | Well presets (`VoidWellNode`): lifetime / capacity / return timing / Dead Circuit growth | **Gravity Well 1.0s · Null Bloom 0.8s r×0.6 · synergy Blackhole 2.5s, absorbs ≤8 shots, returns each 0.15s after absorb · Dead Circuit +25% radius per consumed body, collapse at 3 → burst 150% ATK; max 4 live wells, oldest collapses** | Small numbers first; the overlap cap protects perf and readability. |
| CL-11 | Chill trail durations (Glacial Drift) | ✅ **APPROVED (Brandon, Sep 17): T1 2.0s · T2 3.5s · T3 5.0s +30% size · T4 permanent · T5 arena-wide Ice Rink.** T1–T3 durations are PER TRAIL SEGMENT, measured from its creation. T4's permanent coverage lasts for the CURRENT ARENA, with overlapping ground merged or otherwise bounded. T5 replaces the trails with the rink. | Each early tier gives more territory; T4 changes the strategy from maintaining trails to building a frozen battlefield. |
| CL-12 | Chain Lightning intermediate falloff | ✅ **APPROVED (Brandon, Sep 17), explicitly COMPOUNDED per jump:** each jump retains **75%** of the preceding hit at T2, **85%** at T3; **T4 removes falloff**. | A visible ramp toward the T4 "no falloff" promise. |
| CL-13 | Tesla coil cadence / range | ✅ **APPROVED (Brandon, Sep 17):** T1–T3 coils **140pt range, one shock per 0.8s**. **T4 central coil: arena-wide, one shock per 0.4s** (Brandon: not 0.6 — each shock hits one enemy, so consolidating three coils at 0.6s would LOWER the combined single-target ceiling). | ~60% of Spark's base fire rate per coil so coils support, not replace. |
| CL-14 | Warp Shot acceleration / damage curve | **spawn at 40% speed → 100% over 0.6s; damage 150% at spawn → 100% at full speed (linear)** | "Slower = more damage" with a readable point-blank reward. |
| CL-15 | Red Smile melee arc / Repulse T3 knockback / Aegis spike | **PARTIAL ✅ (Brandon, Sep 18): Red Smile melee arc = 110°, with the approved 80pt reach** — wide enough to catch a group, directional enough that facing still matters. This resolves the melee-angle portion ONLY. **Still pending:** Repulse T3 400pt knockback + 25% ATK on collision · Aegis spike 50% DEF as damage on bounce (both A5). | Starting points only. |
| CL-17 | Boss-class Burn/Bleed damage (A4a) | ✅ **APPROVED (Brandon, Sep 21): 50% for ALL boss-class — the six arena bosses AND mini-bosses** (`GameConfig.BossClass.dotScale = 0.5`). Applied to the FRACTIONAL damage before anything rounds, with remainders carried, so it is a true half. Durations and Crucible stacks are unscaled. Mini-boss Burn (full strength since A1) halves too. **Copy (gate, Sep 21):** Kindle and Bloodthirsty details end "Bosses and mini-bosses take 50% less Burn/Bleed damage." Applied ONCE to the resulting DoT damage, never to application chance, duration, or unrelated damage such as Ember Burst. | Runtime fact: `BossClass.scaledDamage` can't halve a 1-point tick (1 stays 1, 3 → 2), and the approved Bleed tick is exactly 1 at base ATK. |
| CL-18 | Boss Mode DEF dial vs DoTs (A4a) | ✅ **APPROVED (Brandon, Sep 21): DoTs IGNORE the flat DEF dial.** The HP dial, vulnerability and the CL-17 scale still apply. Same rule executes already follow. | The dial is a flat per-hit subtraction (floor 1); DoT damage arrives in arbitrary chunks, so a per-hit cut would gut Bleed's 0.5s ticks and never touch Burn's per-point payouts. |
| CL-16 | Burn re-application / expiry between combat lulls | ✅ **APPROVED (Brandon, Sep 17) — rec, with explicit DORMANT-STACK semantics:** after Burn expires, Burn damage STOPS and one remaining stack is removed every 2s. A new Kindle application REACTIVATES the surviving stacks and resets decay progress, while preserving Crucible's per-enemy 3s stack-addition limit. If no stacks remain, start at one. All timers on paused-aware game time. **Expanded tooltip:** "When Burn ends, its damage stops and stacks fade one every 2s. Reignite the enemy to preserve its remaining stacks." | Keeps stacks from persisting forever on a target that's stopped taking Kindle hits. |

## B2. A4b decisions (opened Sep 21) — ✅ ALL RULED (Brandon × Lyra, Sep 22)

*Full reasoning, runtime facts and the build plan: `docs/v2.1-abilities-a4b-scope.md` §4a. **Unit split (§2):** Red Smile's form change + melee moves to its own unit **A4c** right after A4b; A4b keeps Red Smile on its legacy effect (plus `requires: [.bleedUnlocked]`).*

**Rulings (Brandon × Lyra, Sep 22) — every row is the recommendation EXCEPT CL-26 (amended), with these precise wordings:**
- **CL-19:** `max(1, floor(20% × finishingDamage))` on qualifying kills, overkill excluded; existing barrier cap and expiry kept.
- **CL-20:** ONLY Siphon's overhealing converts, 1:1 after applicable healing modifiers — explicitly SUPERSEDES the earlier broad "all overhealing" wording.
- **CL-21:** bone-white strip, dark-red edge, proportional to barrier/max HP, plus its number. A fully absorbed hit flashes the barrier; a partial absorption ALSO shows the HP damage.
- **CL-22:** independent true-rate multipliers in one shared firing calculation; no new interval floor this unit, subject to the stress gate.
- **CL-23:** preserve leftover shot time with BOUNDED catch-up; pauses and level-up screens must not build firing debt.
- **CL-24:** the HUD shows the live calculated firing rate.
- **CL-25:** 25% vs bleeding enemies and bosses, applied BEFORE fractional DoT accumulation and rounding. Limitation recorded: integer direct hits stay integer (a 1-damage shot still deals 1), so the gate demonstrates the DoT benefit separately.
- **CL-26 — AMENDED:** Exsanguinate activates below **25%** HP. Where it overlaps Execution Protocol, apply **ONE ×2, never ×4**. Execution Protocol keeps its 30% threshold. *Tradeoff recorded honestly:* ×4 was existing behavior, so this is an explicit design amendment; Execution Protocol keeps value through its wider 25–30% window, but owning both stays partly redundant.
- **CL-27:** three fragments, each 30% shot damage (min 1), short range and speed as specified, no crit / pierce / other on-hit procs, guaranteed Bleed on survivors, full fragment kill credit.
- **CL-28:** generations 0 and 1 burst, 2 does not. **Lineage:** a genuine fresh PRIMARY Bleed may establish generation 0 again; a fragment refresh carries its actual generation and never becomes a fresh root. The two-hop limit bounds each lineage; continued player attacks can legitimately seed new outbreaks.
- **CL-29:** the enumerated boss rewards and death-position effects apply once; `killCount` and bat-growth exclusions kept; no player rewards if Spark is already dead.
- **CL-30:** record the boss defeat at the lethal hit (protects the forge-XP bonus through the death animation).
- **CL-31:** the crit-DAMAGE multiplier: add 1.0, preserving Deadeye.
- **CL-32:** bleeding bosses qualify for Open Wounds and Bloodhound; Bloodhound measures to the boss's SURFACE.
- **Defaults approved:** Berserk on live HP (barrier excluded), full-credit reward eligibility, Burn before Bleed, both qualifying death bursts fire together, card ids `v21_berserk` / `v21_sanguinarian`. Barrier expiry keeps its established rule (A0).
- **Packet corrections (Lyra):** (1) Open Wounds is only partly rescued from rounding — see CL-25. (2) Lineage per CL-28. (3) The Glass Blood debug check compares bursts to ELIGIBLE Bleed deaths while Glass Blood is owned (generation and death conditions included), never to all Bleed kills. (4) Boss credit is safe under synchronous cleanup and nested effects: snapshot death position, bleeding state, damage channel, finishing damage and Bleed generation BEFORE resolving rewards; mark the boss credited BEFORE triggering effects; prove the Star clearing `bossStatus` can't erase context or reopen credit, and that a final Bleed tick still qualifies when it also expires the status.
- **Gate-note numbers:** at full HP, Siphon T4 (5) + Sanguinarian (≥1) fill a 50-point barrier in **nine** qualifying kills, not ten. The firing test compares OBSERVED sustained shots/s with the shared calculated rate — the old frame-snap is not a continuing 15/s ceiling after the timer fix.

| ID | Kind | Question | Recommendation |
|---|---|---|---|
| CL-19 | PARAM | Sanguinarian on the real damage scale — a literal 20% of the (remaining-HP-capped) finishing hit is 0.2–0.8 on normal mobs, which rounds to 0 barrier | `max(1, Int(0.20 × finishingDamage))`; bosses 20% of HP actually removed; detail adds "(at least 1)" |
| CL-20 | PARAM | Which heals become barrier with Sanguinarian | Siphon ONLY, 1:1 after the Forge heal multiplier; linked line "With Siphon: Siphon's overhealing becomes Blood Barrier." (keeps health orbs out) |
| CL-21 | ARCH | Barrier tell; a fully absorbed hit currently looks like an HP hit | bone-white strip with a dark-red edge under the HP bar, width = barrier/maxHP, "+N"; a barrier-only hit flashes the strip, not the HP bar |
| CL-22 | PARAM | How Frenzy / Berserk / Bloodlust / Bloodrush combine | each a real-rate divisor folded into `effectiveFireInterval`: `base × mult / Π(1+bonus)`; no interval floor for now |
| CL-23 | ARCH | Fire rate snaps to whole frames (+15% plays as +11%; Bloodlust invisible for ~35 kills) | keep leftover time between shots (`-= interval`, clamped); existing speed builds get a small real buff |
| CL-24 | ARCH | HUD ATK SPD row | show the live rate incl. Frenzy / Berserk / Bloodlust |
| CL-25 | PARAM | Open Wounds: code 15%, approved copy 25%; integer hits hide it | 25%, bleeding enemies AND bosses, unscaled; DoT ticks get it fraction-first |
| CL-26 | PARAM | Exsanguinate: code 30%, copy 25%; stacks with Execution Protocol to ×4 (bosses too) | 25% per the copy; keep the stacking (legacy) — watch the boss last-quarter melt |
| CL-27 | PARAM | What a Glass Blood burst is | 3 short fragments (seed-fragment shape), 30% shot damage (min 1), Bleed survivors 100%, no other on-hit effects, `.fragment` credit, hit + bleed the boss |
| CL-28 | ARCH | Glass Blood chains via Bleed ticks 0.5–3s later | generation lives on the enemy's Bleed; bursts only below gen 2 (Iceburst's cap); lower generation wins on refresh |
| CL-29 | PARAM | Boss-kill effects not yet ruled | Red Harvest, Forge kill triggers, Apex Bloodfed (+1) APPLY; Open Vein / Glass Blood / Null Bloom / Ember Burst / Chain Reaction fire once at the death position; `killCount` and bat growth don't count a boss; no rewards if Spark is already dead |
| CL-30 | ARCH | Existing bug: dying during a boss's death animation loses the boss forge-XP bonus | set `bossDefeatedThisRun` at the lethal hit (the chokepoint) |
| CL-31 | PARAM | Hemorrhage assigns `critMultiplier = 3.0`, erasing Forge Path Deadeye | bug fix: `+= 1.0`, copy unchanged; A7 logs "needs a crit source" |
| CL-32 | PARAM | Bleeding bosses in the rest of the tree | Open Wounds gets a boss arm; Apex Bloodhound treats a bleeding boss as a candidate (nearest bleeding target wins) |

## B3. A4c decisions (opened Sep 22) — ✅ ALL RULED (Brandon, Sep 22); A4c ✅ SHIPPED to main Sep 23 (Red Smile form change + melee)

*Scope: `docs/v2.1-abilities-a4b-scope.md` §2 (A4c contents) and §4b (the 12 drafted questions a–l, re-checked here against source and extended with the items the handoff says must be ruled explicitly: terrain, swing cadence, and what "active combat" does to Grounded Core). Approved inputs this batch does NOT reopen: Q-B4 (10s start-to-start in active combat, 3s form, ~80pt reach, attack-speed cadence, hits all in the arc, 2× projectile damage as Void, 100% Bleed, existing projectiles live, new firing pauses, dual-signature gate) and CL-15 (110° arc, 80pt reach). Nothing below is built until ruled.*

**Runtime facts from the A4c recon (HEAD `b29ccc1`, Sep 22). Each one changed or sharpened a row:**
1. **"Active combat" has one consumer today, and A5 will add the second.** At HEAD, Grounded Core is a 0.7s brace with no combat condition, and Fortify is a global 8% slow. The stand-still and in-combat versions exist only in the spec (A5). So the definition picked here is also A5's.
2. **The gun's target picker is not a clean "in combat" signal.**
   - Its 400pt range is not DeviceScale-scaled, and it grows with range cards.
   - It prefers the Skybeam lasso target.
   - It happily targets a phased PaneStalker or the vanished Faceted Lie. The shots just pass through.
   - It already holds fire when every enemy is behind the Carrier.
3. **Nothing marks intangibility except `physicsBody.categoryBitMask = 0`.** PaneStalker's phase and the Lie's vanish are both private, and death uses the same mask. Every non-physics effect already lands on both: DoTs, area bursts, the kaiju strike. The Lie's DoT-through-vanish is commented as intended. The sweep would be the first non-physics effect that deliberately respects intangibility.
4. **`lastMoveDirection` works as a facing vector, except it is never reset between runs.** It is unit length, starts straight up, updates only on stick input outside the dead zone, and survives release, pause and the boss reveal. Because `restartGame` never touches it, a new run faces wherever the last run ended.
5. **The kaiju runs every 55s start-to-start** (10s form plus a 45s gap) with no combat condition, on raw `dt` counters. While it is active it freezes the gun's `FireClock`, but nothing else: turrets, flowers, Iron Maiden and echoes keep going.
6. **Braceguard's shield tests where Spark stands** (`blocksHit(from: player.position)`), so a frontal sweep is exactly the case it halves. The only bypass today is Erasure's `erasureVoidTouched`.
7. **The projectile hit chain has about 25 inline steps.** The card copy splits them into two kinds: Kindle, Frost Touch and Repulse say "**Projectiles** …", while Overload, Whiteout and Chain Lightning say "**Hits** …".
   - The Apex and Erasure meters already have per-stack cooldowns, so registering once per target can't flood them.
   - `applyForgeOffense` has per-call side effects. Cold Fury and Killing Stroke charges are spent on the first target. Relentless resets whenever the target changes, so in a sweep it would reset on every hit.
8. **Crit and damage are frozen when a projectile is FIRED**: crit is rolled at fire time, and `damageMultiplier` includes Overcharge at fire time. A swing would read both live, per target.
9. **Radii:**
   - Enemies have no combat radius. The physics body is always 12pt, even on a mini-boss drawn at 2.2×.
   - Mobile bosses report `targetingRadius = 0`, so the gun and the kaiju measure them to their centre. Their bodies are 21–36pt.
   - The Unmade Star's surface radius is about 335pt.
10. **The Carrier is ~177pt thick** — *corrected in review:* the Splitworks radius is 350 × 1.15, and the rounded corners add 0.06r a side, so 0.44r ≈ 177pt, not 112. Against an 80pt reach, the terrain rule only bites around its rounded ends. **Also found in review:** the shared `segmentBlocked` marches at the footprint's half-thickness (~88pt), so any shorter segment is only tested at its endpoints and passes the rounded ends unblocked. The sweep therefore uses a new exact test (`segmentBlockedExact`); the shared one is unchanged (see the A4c return packet).
11. **Void's card colour is purple (0x9944CC).** In-world purple is locked as DANGER (enemy shots and ranged enemies).
12. **`KillSource.sweep` already exists** as the reward-only dying-enemy prune, so the new case has to be `.melee`.
13. **Base numbers:**
    - fire interval **0.5s**
    - projectile damage **1** (`damageMultiplier` 1.0)
    - `shotFractionDamage(2.0)` = **2**
    - crit ×2.0
    - Bleed tick **1 per 0.5s for 3s**

    A 3s form at base swings **6 times**.

**FINAL RULINGS (Brandon, Sep 22). These WIN over the draft table below wherever they differ.** Recon findings stay authoritative inputs. Approved: CL-34, 35, 36, 37, 38, 40, 41, 43, 45, 46, 47 (some with a clarification or default). **Modified:** CL-33, 39, 42, 44, 48. Scope: implement A4c only; no A5, A6, A7 or unrelated cleanup.

- **CL-33 Active combat — MODIFIED.** Active combat means **at least one currently hittable hostile enemy or boss exists in the active encounter.**
  - Distance, line of sight and gun targeting range do NOT matter.
  - Phased or vanished enemies do NOT qualify.
  - After the last qualifying hostile stops qualifying, active-combat state holds for **0.5s** before dropping.
  - Hiding behind the Carrier is NOT leaving combat while valid hostiles still exist.
  - Shared infrastructure: Red Smile now, Grounded Core in A5.
- **CL-34 Cycle — APPROVED.** A 10s start-to-start cycle, measured only during active combat.
  - The first transformation comes after 10 accumulated seconds of active combat.
  - Lulls pause the cycle; they never reset it.
  - The cycle restarts at each transformation start.
  - The transformation always gets its full 3s, unless another approved mechanic (the kaiju) ends it.
- **CL-35 Facing — APPROVED WITH DEFAULT.** Normalized `lastMoveDirection`, which holds where the player last moved when the stick returns to neutral.
  - Reset every run, so it can't carry across restarts.
  - Default is the deterministic up-screen `(0, 1)` until the player gives movement input.
- **CL-36 Damage — APPROVED WITH CLARIFICATION.** Each sweep target receives `shotFractionDamage(2.0)`: twice the current projectile/shot damage, **not** 200% ATK.
  - Crit rolls independently per target.
  - Every surviving target gets guaranteed Bleed.
  - Bosses and mini-bosses get the same damage basis as ordinary targets, with no special Red Smile penalty.
  - Ordinary applicable direct-hit defenses still apply, unless an explicitly approved Red Smile/Void rule bypasses them.
- **CL-37 Void damage — APPROVED.** In A4c, Red Smile's Void damage has ONE special consequence: it bypasses the Braceguard directional shield. Void affinity does not automatically bypass unrelated defenses, terrain, walls, collision or other mechanics.
- **CL-38 Terrain — APPROVED.** Red Smile does not sweep through the Carrier; its geometry blocks the attack normally. This is independent of Void affinity.
- **CL-39 Hit semantics — MODIFIED.** A sweep is a **primary attack**, and each enemy struck receives a **primary hit**. A swing is **not a projectile** and **not a shot**. Therefore:
  - Hit-based, primary-hit, and hit-qualified status/damage effects may trigger per target.
  - Projectile-only effects do NOT trigger.
  - Shot counters do NOT advance.
  - Effects that spawn or modify additional projectiles because a shot occurred do NOT trigger.
  - Forge offense charges and Apex/Erasure-style hit meters keep their normal per-hit semantics, so striking several targets may advance them several times where their contract is hit-based.
  - **Chain Lightning triggers at most once per swing**, however many targets are struck.
  - Classify effects by their text/contract semantics; never infer eligibility from where the code happens to sit.
- **CL-40 Phased/vanished — APPROVED WITH GUIDANCE.** Red Smile never strikes phased or vanished targets.
  - Use an explicit SHARED hittability state/predicate, suitable for both Red Smile and CL-33, rather than Red-Smile-specific logic.
  - Do not repair the gun's targeting of phased/vanished actors in A4c unless compilation or contract integrity requires it. Track it separately.
- **CL-41 Reach and arc — APPROVED WITH GEOMETRY CLARIFICATION.** 110° arc, 80pt reach, with **body-radius-aware sector overlap**.
  - Reach is measured against the target's occupied body, not its centre, and angular overlap naturally accounts for wide targets.
  - No per-boss special cases, and no arbitrary global angle expansion for size.
  - The Unmade Star and other wide actors are hit when their physical footprint genuinely overlaps the sweep sector.
- **CL-42 Swing cadence — MODIFIED.** Red Smile owns a SEPARATE melee clock that reads the SAME shared effective attack-rate calculation as the gun, so all approved attack-speed modifiers apply.
  - The first swing comes immediately when the form begins; later swings follow the effective interval. That gives **six swings in a 3s form at the 0.5s base**.
  - Empty swings still happen.
  - Keep a bounded timing remainder as appropriate.
  - **NO 0.10s minimum interval in A4c.** The true-rate philosophy stands: stress-test the uncapped calculation first, and add a floor only with device/performance evidence plus Brandon's approval.
- **CL-43 Gun during the form — APPROVED.** Primary auto-fire pauses. Its firing-clock remainder freezes, with no shot debt accruing while paused, and the gun resumes from that preserved state when the form ends.
  - Do not disable `fireProjectile` globally.
  - Existing projectiles, fragments, echoes, turrets, summons and other independent sources continue under their existing rules.
- **CL-44 Kaiju overlap — MODIFIED.** The kaiju has priority.
  - If the kaiju activates during Red Smile, Red Smile ends immediately and the kaiju proceeds normally.
  - While the kaiju is active, the Red Smile cycle clock pauses, and no transformation begins, banks or queues.
  - When the kaiju ends, Red Smile resumes from its remaining cycle time if active-combat conditions are satisfied.
- **CL-45 Tree identity — APPROVED.** It counts as both a Bleed and a Void card and feeds both ladders. It stays one card and one pool entry, with no count increase. Eligibility still requires both unlocks.
- **CL-46 Defense and movement — APPROVED.** Attack mode only. It inherits none of the kaiju's slowdown, damage reduction or other defensive behaviour.
- **CL-47 Placeholder presentation — APPROVED.**
  - Near-black Spark with a crimson grin.
  - A readable facing indicator/arc.
  - A crimson crescent per swing.
  - A lightweight RED SMILE timing/countdown treatment readable on device.
  - **No Void purple on the transformed body** (purple = danger).
  - Final production art is a later art/closeout concern.
- **CL-48 Card text — MODIFIED.** Face: **"Every 10s: 3s of Void melee. 2× damage + Bleed."** Detail = the previously approved copy, then: **"Sweeps strike in your last move direction. They count as primary hits, not shots or projectiles."** Never use broad copy like "carry your on-hit effects".

*Draft recommendations as submitted (superseded where the rulings above differ):*

| ID | Kind | Question | Recommendation | Why |
|---|---|---|---|---|
| CL-33 | ARCH | **What "active combat" means** (shared; A5 Grounded Core reuses it) | **One per-frame flag computed in `update()`: true while a live, tangible enemy or boss is within 400pt × DeviceScale.gameplay of Spark, with line of sight in geometry arenas (the Carrier blocks it), with a 0.5s linger so a single flicker doesn't toggle it.** Measured to the surface. The value lives in `GameConfig.Combat`. **Not** "the gun has a target": range cards, the lasso and intangible targets would all change it. | Same idea as the gun, but decoupled. **Consequences, checked:** in open arenas it's true whenever something is nearby, and false in a cleared-field lull. In the Splitworks, hiding behind the Carrier from everything means *not* in combat, so Grounded Core can't bank DEF behind cover and Red Smile's clock waits (matches the A4b device read that ranged pressure beats turtling). In the Star Anvil, enemies beyond 400pt don't count. |
| CL-34 | ARCH | **The 10s cycle** | **The clock advances only while in active combat. Lulls pause it; they never reset it.** At 10s the form starts and the clock restarts from 0 at that START, so it's start-to-start and the gun gets 7s. The form always runs a full 3s of game time, even if combat ends during it. **The first form comes after 10s of combat once the card is taken.** Death, revive and restart end the form and zero the clock. All timers run on `GameTimer`, ticked from `update`. | "Start-to-start" per Q-B4. A form that always runs out keeps the tell predictable. Starting at 0 means picking the card doesn't transform Spark the instant the level-up closes. |
| CL-35 | ARCH | **Facing** | **`lastMoveDirection`**: the last stick direction, held while standing still. **Also reset it to "up" in `restartGame`** (fact 4). | "Directional enough that facing still matters" (CL-15) means the player aims by moving. Auto-facing the nearest enemy would erase that choice. |
| CL-36 | PARAM | **Damage** | **`shotFractionDamage(2.0)`, read live at swing time: 2 at base.** Not 200% ATK, which would be 20. **Crit is rolled per target at swing time.** **Bleed 100% on SURVIVORS, generation 0 (a fresh primary root, CL-28), through `inflictBossBleed` on a boss.** Direct melee hits on a boss take the Boss Mode DEF dial like gun hits. **Mini-bosses and bosses take full damage; there is no BossClass scaling** (Red Smile isn't a capstone, and the gun isn't scaled either). | The approved "200% of projectile damage" is the projectile scale. Survivor-only Bleed keeps the "bleeding before the killing damage" invariant. |
| CL-37 | ARCH | **What "Void damage" means in A4c** | **A hit flag with exactly ONE effect this unit: it ignores Braceguard's shield** (the Void-Touched precedent, fact 6). Terrain is CL-38's call, not a property of Void. A6 attaches Phase T2 and Anomaly to the same flag later. | Without the bypass, every frontal sweep into a Braceguard is halved, the exact case melee creates. |
| CL-38 | ARCH | **Does the sweep reach through the Carrier?** *(explicit ruling required: handoff)* | **No. Each candidate needs a clear segment from Spark (`segmentBlocked`, projectile travel radius). A sweep does not reach through solid geometry.** Open arenas skip the check. | Melee is damage along a short segment, which Ruling 1's behavioural test says the Carrier blocks. Nothing about the placeholder reads as "violating space". The Carrier is 112pt thick vs 80pt reach, so this only matters at its ends. **Alternative:** classify the Thing From Below as a supernatural manifestation that passes, which would need a visual that sells it. |
| CL-39 | ARCH | **Which on-hit effects a swing carries** | **Principle: each sweep hit is a PRIMARY HIT on that target, but a swing is not a SHOT.** **Carried per target:** crit (+Lucky Break), both executes (one ×2, CL-26), Permafrost, Brittle Cold, Open Wounds, Forge offense (with **Relentless not fed**: `relentlessTarget: nil`), Void Braceguard bypass, Kindle Burn + Crucible, Frost Touch, Repulse knockback, Overload, Shatter, Whiteout, seed embed (as generation 0), Forge Overkill, and the Apex/Erasure meters (already cooldown-limited). **Chain Lightning: ONE chain per swing**, from the first target hit, at that hit's damage. **Not carried:** Storm Engine, the glacial/icicle counter, Calculated Strike, Erasure Echo, Fracture, Mirror Edge, icicle shatter, and Induction (dormant). Other cards' copy is unchanged; Red Smile's detail says sweeps carry on-hit effects. | "Replace projectile attacks with melee" should not turn the bridge into an anti-synergy with every other tree's on-hit identity. One chain per swing matches one chain per shot hit; per target would be 8 chains per swing. **Alternative for Chain:** none during the form (the §4b draft). |
| CL-40 | ARCH | **Phased or vanished targets** | **Add an explicit `isIntangible`** (PaneStalker while phased, the Faceted Lie while vanished; default false everywhere else, bosses included). The sweep and the CL-33 flag both skip intangible targets. DoTs and area effects keep landing as today, and **gun targeting is unchanged this unit** (fact 2 is logged for A7). | Sweeping a boss that has visibly left the world reads as a bug. Scope stays on the new effect. |
| CL-41 | PARAM | **Reach and the arc test** | **Reach 80pt × DeviceScale.gameplay, measured to the target's SURFACE. Arc 110° (±55° from facing), widened by each target's angular radius (asin(r/d))**, so a big body counts when any of it is inside the arc. **Radii:** enemies 12 × their node scale (a mini-boss counts as ~26); bosses by their body radius: the Star's 335, and new per-boss values from each physics body for the five mobile bosses. All in `GameConfig.RedSmile`. | Approved reach and angle. Without surface distance and widening, a wide boss and the Star would be missed while visibly inside the swing. |
| CL-42 | PARAM | **Swing cadence** *(explicit ruling required: handoff)* | **Its own `FireClock` instance at `effectiveFireInterval`**: every attack-speed source applies (Static/Surge/Glass Engine, Frenzy, Berserk, Bloodlust, Bloodrush), with the bounded carry. **Multishot adds nothing**, since one sweep already hits everything in the arc. **The first swing is on the form's first frame, then one per interval while the form runs** (6 at base). **Empty swings still happen on cadence**, so the arc FX doubles as the facing tell. **Floor: no swing faster than every 0.10s.** | Shared rate infrastructure, not a parallel speed model (handoff). The floor is new and optional: the gun has none (CL-22), but a stacked build reaches 15/s, and 15 crescents a second × 8 targets of full on-hit chains is where readability and frame time go first. |
| CL-43 | ARCH | **The gun during the form** | **Its clock FREEZES, like the kaiju: it neither advances nor resets.** Echoes already scheduled may still fire. Turrets, flowers, summons, Iron Maiden and every other source are unaffected. | Q-B4: existing projectiles live, NEW firing pauses. Same mechanism as the kaiju, one code path. |
| CL-44 | ARCH | **Kaiju overlap** | **The kaiju wins.** If it begins mid-form, Red Smile ends at once, and the rest of that form is lost. Red Smile's clock is frozen while the kaiju is active. | Two body forms can't both draw. The kaiju is the rarer, bigger spectacle. |
| CL-45 | PARAM | **Void secondary tag** | **`secondaryTag: .voidT`**: the first pick counts toward BOTH the Bleed and Void ladders, like Silver Skin and Deeproot. The Bleed card count stays 13 (primary tag) and the pool stays 79. | It's the Bleed/Void bridge. The dormant-bridge rule already agrees with `requires: [.bleedUnlocked, .voidUnlocked]`. |
| CL-46 | ARCH | **What the form does besides attack** | **Offense only:** movement speed, hurtbox, damage taken and pickups are all unchanged. (The kaiju's 0.7× move and 85% damage reduction are NOT borrowed.) | Nothing approved says otherwise. Defensive riders would be a new design. |
| CL-47 | ARCH | **Placeholder tells** | **Body:** Spark's body goes near-black, with a crimson grin and a faint crimson 110° wedge at low alpha showing the facing arc for the whole form. **Each swing:** a crimson crescent over the arc. **Start/end:** a short flash plus existing sounds. **HUD:** a capstone-timer row "RED SMILE" counting down to the next form, reading ACTIVE during it. **No purple anywhere in-world** (fact 11). Lyra's Thing From Below replaces it in A9. | Kickoff rule: a readable tell ships with the mechanic. A 10s aimed ability needs to be seen coming. |
| CL-48 | PARAM | **Copy** | **Face (fits 17×3 beside MORE):** "Every 10s: 3s of Void melee, 2× + Bleed". **Detail:** the approved sentences verbatim ("Every 10s in combat, become the Thing From Below for 3s. Replace projectile attacks with sweeping melee attacks that deal 200% of projectile damage as Void damage and always inflict Bleed. Requires both Bleed and Void signatures. Existing projectiles remain active during transformation."), **plus two proposed:** "Sweeps strike the way you last moved and carry your on-hit effects." | The two added sentences state the rules a player can't otherwise discover (CL-35, CL-39). |

**Gate-question rulings Q1–Q7 (Brandon, Sep 23).** From the build's independent review; full text in `docs/v2.1-abilities-a4c-return.md`. Each confirms what is built.
- **Q1 Calculated Strike: KEEP.** One swing is one direct attack, and every 5th swing crits every body it strikes.
- **Q2 Mirror Edge: EXCLUDED.** A swing is not a shot or a projectile, so there is no projectile echo.
- **Q3 Same-swing Bleed, then a Chain kill: KEEP.** Normal timing: Bleed applied before the chain's killing damage counts as bleeding before the killing damage.
- **Q4 Relentless: ACCEPTED AS RULED.** Per-hit reset, even across a multi-target sweep.
- **Q5 12pt runtime hit body: KEEP.** Use the authoritative runtime hit body, never sprite width, and log visual/hitbox mismatches separately.
- **Q6 Chain onto phased targets: A7.** Logged as "secondary-chain targeting does not respect shared hittability"; A4c doesn't touch the shared Chain targeting.
- **Q7 Restart-countdown-HUD fix + kaiju-death scale reset: KEEP BOTH.** They are bounded compatibility fixes required for A4c's lifecycle and presentation.

**Also in A4c (build mechanics, not decisions):**
- `KillSource.melee`, with full credit.
- Remove the legacy low-HP hook: `bleedLowHpBonus` and `bleedLowHpThreshold`, `EnemyNode.bleedDamageMultiplier`, and `StatusDoTs`' `bleedMultiplier` parameter. That touches 6 source sites and the bleed-harness D4 check plus 8 call sites.
- The atlas dual-requires regex, which today silently drops the card's "needs ▸" chip.
- A pure arc/form state type with its own harness.
- The Red Smile card keeps its id `v18_red_smile`, so there is no Codex migration.

- Overcharge cap/reset ✅ (CL-6).
- Fragment sources traced ✅ (CL-3).
- Damage ordering documented ✅ (CL-5); flat DEF floor of 1 damage means DEF alone cannot produce invulnerability.
- Kill-credit ownership: `onEnemyKilled` is the single chokepoint today; the build's shared-events unit adds a `KillSource` so explosions, Bleed deaths, returned shots, chains and melts can't double-credit or recurse.
- Geometry contract: coils, teleports, fear movement and wells all route through `PlacementSampler` / `resolve` / the 1B steer target — nothing new required.
- Card counts recomputed: 80 after the pass (Void is 12→13, not 12→12).

## D. CL-5 — runtime classification and the resulting formula (Sep 15)

Every operation in today's `applyPlayerDamage` / `PlayerStats.takeDamage`,
from source, in the order it runs:

| # | Operation (today) | Source | Exact op | Activation | Classification |
|---|---|---|---|---|---|
| 1 | Boss Mode ATK dial | `BossModeDials.atk` | `dmg *= atk` | Boss Mode only, boss-class sources only | **Input scaling** (stays first, outside the ceiling) |
| 2 | **Unyielding** (Forge Path 20B) | `GameConfig.ForgePath` | `dmg *= 0.5` | node owned AND cooldown ≤ 0 AND the RAW hit > 20% max HP; then 45s cooldown | **Percentage reduction, conditional, once per 45s → INSIDE the 90% ceiling** |
| 3 | Forge Path DR bucket | Vital Surplus 5% (above 75% HP) ⊕ Last Stand 10% (below 35%) — a fork, mutually exclusive; Hold the Line 8% (5+ near) ⊕ Giantkiller 10% (boss-class) — a fork; Braced Impact 25% (first hit per 12s) | `dr = min(Σ, 0.6); dmg *= 1−dr` | per-node conditions | **Percentage reduction, additive inside its own bucket, cap 0.6** (practical max 0.45) → treated as ONE factor inside the ceiling |
| 4 | **Kaiju** (Panda tree transformation) | `GameConfig.Panda.kaijuDamageReduction = 0.85`, `kaijuDuration = 10s` | `dmg *= 0.15` | while `kaijuActive` (10s window) | **Percentage reduction (85%)** — today applied AFTER the bucket cap on purpose (source comment: "a transformation, not another entry in the mitigation bucket"). See the exception question. |
| 5 | Flat DEF | `DEF + Fortify temp + Grounded Core + Deeproot ground DEF (+ Ironhide's +10 today)` | `dmg = max(1, dmg − DEF)` | always | **Flat subtraction with a floor of 1** |
| 6 | HP | — | `HP −= dmg` | — | health damage |
| 7 | Second Breath (20A) | `ForgePath` | heal 10% max HP | not dead AND below 25% AND cooldown ≤ 0 (45s) | **Heal after damage** — not a reduction; unchanged |
| — | Phase Skin / i-frames | `invulnerableTimer`, `isPhaseSkinActive`, `damageCooldownTimer` | the contact is IGNORED before any of the above | on trigger | **Explicit invulnerability** — separate, pre-pipeline; unchanged |
| — | Brace | today: survives one lethal hit at 0 HP | — | once/run | **Lethal rescue** |

**The one exception question (design decision, not mine to make):** kaiju.
Inside the ceiling, kaiju alone still takes only 15% of every hit (0.85 <
0.90), so the transformation loses nothing on its own. It only loses when
STACKED with other reductions: today kaiju × Ironhide-90% would be 98.5%
taken-off; under the ceiling it is 90%. **RULING (Brandon, Sep 15): no exception —
kaiju goes inside the ceiling.** (Recommendation adopted as an explicit
recorded ruling, not a default.) "Pandas may violate geometry as
spectacle, not as save-state corruption" is the same instinct: the
spectacle is intact at 85%, and a 98.5% stack is where practical
immortality lives. If Brandon wants the kaiju window to stay above the
ceiling, the exact exception would be: *"kaijuDamageReduction is applied
after the ceiling, as a transformation multiplier, for its 10s window."*

**Resulting CL-5 formula (A0 builds exactly this):**

```
raw                                                  // enemy's hit
if gauntlet && bossClass: raw *= BossModeDials.atk   // 1. input scaling (unchanged)

// 2. percentage layer — multiplicative, global ceiling 0.90
forge    = min(0.60, Σ active Forge Path DR sources) // one factor, additive inside
unyield  = (owned && cd ≤ 0 && raw > 0.20·maxHP) ? 0.50 : 0   // consumes its 45s cd when it fires
ironhide = crowd-scaled 0…0.90 (rework model; replaces today's flat +10 DEF)
aegis    = 0 / 0.25 / 0.35 by tier
kaiju    = kaijuActive ? 0.85 : 0                    // RULED Sep 15: inside the ceiling, no exception
R = 1 − (1−forge)(1−unyield)(1−ironhide)(1−aegis)(1−kaiju)
R = min(R, 0.90)
dmg = raw × (1 − R)

// 3. flat DEF, floor 1
dmg = max(1, dmg − (DEF + fortifyTemp + groundedCore + deeprootGround))

// 4. barrier before health
absorbed = min(barrier, dmg); barrier −= absorbed
toHP     = dmg − absorbed
fire "hit" events (Overcharge reset, Defiant, Slipstream clock) whenever dmg > 0,
  with (absorbed, toHP) reported so reactive effects pick the right event

// 5. health + rescues — compute lethal, then rescue, THEN finalize
HP −= toHP
if HP ≤ 0:
    if braceAvailable:        HP = 1; braceAvailable = false          // first
    elif unbrokenAvailable:   HP = 1; unbrokenAvailable = false;      // second
                              invuln 10s; bonusATK = DEF for 10s
    else: death
    // a barrier-only hit has toHP == 0 and can never reach this branch
// 6. Second Breath heal (unchanged, after survival is known)
```

Explicit invulnerability (Phase Skin, i-frames) stays where it is: the
contact is ignored before step 1. Enemy boss-ATK scaling stays input
scaling. Flat DEF's floor of 1 prevents DEF alone from zeroing a hit; it
does not claim practical immortality is impossible once sustain stacks —
that is a playtest question, as the packet says.
