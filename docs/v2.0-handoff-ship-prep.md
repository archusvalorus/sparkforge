# Sparkforge v2.0 — Handoff: paused, awaiting a submission slot

*Rewritten Jul 28 2026. This doc previously said "FEATURE COMPLETE and NOT SHIP
READY" and listed three blockers. **All three are cleared.** Read `MEMORY.md`
first, then this.*

---

## The one-line status

**v2.0 is done and waiting on Apple, not on us.** Built, tagged, archived and
uploaded. The only remaining action is pressing submit, and that is blocked by
App Store Connect rate-limiting review submissions across the whole account.

| | |
|---|---|
| Tag | **`v2.0`** at commit `fb2b900` |
| Build | 2.0 (6) |
| Archive | uploaded to App Store Connect |
| Device pass | **PASSED** on a physical iPhone 15 |
| Store assets | screenshots retaken, What's New written |
| IAPs | all three created in ASC, batched, pending |
| Submission | **BLOCKED — Apple's queue, nothing else** |

**Archive v2.0 from the `v2.0` tag, not from `main`.** Once v2.0.5 work starts,
`main` stops being the submission source.

---

## What to do when the queue clears

1. Submit. Notes and screenshots are already in place.
2. On approval: flip the live version in `CLAUDE.md` (line 3) and in the Notion
   App Portfolio Registry. The registry already carries v2.0 as *built, not
   live* — just move it.
3. Then v2.0.5. See below.

---

## Cleared this session (was the blocker list)

- **Version** bumped 1.9 → 2.0, build 5 → 6.
- **`.storekit` config** added, and Xcode shared the scheme when it was wired,
  so the StoreKit pointer now lives in version control rather than `xcuserdata`.
  It survives a fresh clone. The reference sits only in `LaunchAction` and can
  never reach a Release build.
- **All three premium IAPs** created in ASC. Details, copy as actually entered,
  and the hard-won field rules are in `docs/v2.0-asc-iap-setup.md`.
- **Device pass PASSED**, the studio's own final gate.

## Fixed this session — worth reading, all found by PLAYING

Three unlock bugs, none of which could fail loudly, all surfaced by playing as a
new user rather than by reading code:

- **The panda skin had no unlock call anywhere.** The card set `pandaTier` 1..5
  and stopped. The secret tree had no payoff at all.
- **Star-Crossed was gated behind the Mote**, whose encounter needs 10,000
  lifetime kills. Clearing Arena 5 granted nothing, contradicting the skin's own
  blurb. Arena 4's boss had always granted Ascended correctly; Arena 5's line was
  simply missing.
- **Flame Panda was unpurchasable.** The hub only makes a *revealed* family
  tappable, and a secret family revealed only by owning a member. With the panda
  skin unreachable, the family could never open, so the $9.99 IAP could only be
  bought from inside a room you could only enter by already owning something in
  it. Discovered because the ASC review screenshot couldn't be taken.

Also: the skin picker drew palette only, so every premium skin looked like a
recolour of its free sibling at the point of purchase and Flame Panda previewed
as a plain orange dot. Previews now compose overlays and sprites. And the pause
menu's MENU button quit instantly with no confirmation.

---

## Still never observed running

Built, committed, and still unverified in play:

- The **Panda Samurai** (5% at T4+)
- **Mote** end-to-end (gated at 10k lifetime kills; the save is far below)
- The **Rabbit** sprite
- The ordinary-panda **re-cut walk order**
- The **kaiju walk/attack animation** — a look was intended during the last
  session but no result was reported, so treat it as unconfirmed

Confirmed since the last handoff: both premium skins, the procedural overlays
(flames and constellation), the pale-gold Star-Crossed eyes, the panda skin and
its face, and all three unlock paths.

Most of what remains is deliberately rare, so natural play won't reach it. Plan
one forced-seam pass purely to LOOK at it.

**Most likely to need tuning:** frame and flicker timings. Every one was chosen
by reasoning rather than by watching — panda walk `0.18`, lion prowl `0.14`,
samurai strike `0.30/0.10/0.22`, kaiju flame flicker in `makeOverlay`. All
constants, all in `GameConfig`.

---

## Debug seams (verify COLD before any push)

| Seam | File |
|---|---|
| `Panda.debugAlwaysEligible` | GameConfig |
| `Mote.debugForceEntrance` | GameConfig |
| `BossMode.debugForceFullRoster` | GameConfig |
| `BossMode.debugStartStage` | GameConfig |
| `debugForcedCardID` | UpgradeManager |
| `SkinManager.debugUnlockAll` | SkinManager (`#if DEBUG`, runtime toggle, not persisted) |

**Reinstall the sim after flipping one back** — a stale build bit us repeatedly.
And **don't `git add -A` while Brandon is mid-playtest**; that swept a hot seam
into a commit once already. Stage by name.

---

## Next: v2.0.5, then v2.1

**v2.0.5** ships right after v2.0 clears review. Deliberately more than skins —
a store-only release trains players to skip updates. Two items are already
specced with their open calls written down:

- `skybeam-rework-v21` in memory — **T5 strike only**, T1–T4 untouched
- `thornwall-true-damage-v21` in memory — genuinely a one-constant change

**v2.1** is the Geometry Arc, arenas 6–10. **Marketing is paused indefinitely**
(Brandon, Jul 28) until the first 10-level arc feels really good, which means
v2.1 gets to be as experimental as it needs to be with no launch expectations
riding on it.

---

## Operating discipline (reusable, not v2.0-specific)

- **`docs/character-art-playbook.md`** — the mold. Size before commissioning,
  points not pixels, body-raster/effects-procedural, animate by screen time,
  union-box cropping, and the warning that animated sprites do not render at the
  size the arithmetic predicts.
- **`docs/art-briefs.md`** — what was asked for and the creative intent.
- **`art/source/<character>/`** — drop folders with the naming convention.
  Sources committed so assets can be re-cut without Lyra.
- **`docs/v2.0-asc-iap-setup.md`** — App Store Connect field rules, learned the
  hard way. Read before touching IAPs again.

**Skin tier rule:** earned skins RE-TINT; premium skins add FORM and FIRE.

**Sprite vs procedural rule:** does it need to keep being Spark? Yes → palette or
overlay. No → sprite.

**Face rule:** anything that reads as part of the face rides `eyesNode`, so it
travels with the eyes. Static features under darting eyes read as a second, dead
pair of eyes.

**Secrets rule:** gate on discovery, not on ownership. You cannot spoil a secret
for someone who has already found it.

All of the above are also locked in the Notion Game Design Decision Log under
*Sparkforge v2.0 "The Arrival"*.

---

Open, unrelated: the kaiju renders ~3x larger than its constants claim and that
appearance is **LOCKED** — do not "fix" it (see the warning in `setKaiju`).
