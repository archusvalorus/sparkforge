# v2.1 Work Tracks — the three-thread split

*Brandon + Claude, Aug 2026. v2.1 is a WoW-patch-scale release run by three
partners; one conversation thread can't hold it. Three tracks, each its own
Claude thread, all reading THIS repo's docs as shared state.*

## Ground rules (continuity without collisions)

1. **The repo is the shared brain.** Every unit ends with its return packet
   or spec update in `docs/` + a commit. A thread orients by reading:
   `CLAUDE.md` → its own track doc → the reconciliation docs it cites.
2. **One track active at a time on the working tree.** Threads isolate
   CONTEXT, not files — Brandon's validation gates serialize the actual
   edits naturally (he is the bottleneck, by design). No simultaneous
   edits from two threads.
3. **Commit prefixes name the track**: `feat(v2.1-geo):`, `feat(v2.1-abil):`,
   `feat(v2.1-content):` — history stays legible.
4. **Shared-file coordination:** `GameConfig.swift`, `UpgradeManager.swift`,
   and `GameScene.swift` are touched by every track. The rule: a track
   only edits the sections its charter names; anything cross-cutting gets
   a line in this doc's "cross-track notes" before it lands.
5. **Memory:** all threads share the project memory. Each thread updates
   only its own track's clause in the index line.

## Track 1 — GEOMETRY (the original thread, context warm)

Charter: the arena geometry engine. Unit 1B route graph → Unit 2's
geometry-side policies (projectile blocking, auto-aim/summon LoS, legacy
shooter behavior). Owns: `ArenaGeometry.swift`, `GeometryDebug.swift`,
routing/steering seams in GameScene.
Gates: Brandon's 1A device feel pass (open) → authorize 1B.
Docs: `arena6-geometry-reconciliation.md` (governs), `v2.1-geometry-1a-return.md`.

## Track 2 — ABILITIES (spawn now)

Charter: the card/ability rework + signature sub-system + canon amendment.
Owns: `UpgradeManager.swift` card pool, `PlayerStats`, capstone update
functions, the atlas tool. First unit (buildable TODAY, no open calls):
the signature draw mechanics — `isSignature` flag, the zero-signature
spread guard, Lyra-rule reroll. Then per-tree builds as Brandon's ~28
open-call answers + Neutral notes land.
Docs: `v2.1-card-rework-spec.md`, `v2.1-signature-cards-spec.md`.
Opening prompt: *"Abilities track for v2.1. Read docs/v2.1-worktracks.md,
then docs/v2.1-signature-cards-spec.md and docs/v2.1-card-rework-spec.md.
First unit: build the signature draw mechanics per the locked answers in
the signature spec §4. One unit at a time, DoD gates, Brandon validates."*

**Status Sep 15 2026:** decision packet landed (`docs/v2.1-ability-decisions-handoff.md`), spec reconciled, closure table + kickoff written (`docs/v2.1-ability-closure-table.md`, `docs/v2.1-abilities-kickoff.md`). Open the Track 2 thread with: "read the worktracks doc, the abilities kickoff, and the closure table; start at Unit A0 once CL-5 is answered."

## Track 3 — CONTENT (spawn after geometry Unit 1B + Unit 2 policies)

Charter: the Splitworks roster (Spurhound / Linekeeper / Ramplate), the
Marchwarden, Marchworn skin, Arena 6 bestiary, art/sound integration,
Boss Mode compat. Consumes the route graph + LoS + spawn zones; cannot
start before they exist.
Docs: `arena6-splitworks-design-lock.md` §5–§7, reconciliation §5 Units 2–4.

## Standing items (any thread absorbs)

- BGM tracks: 2 per arena (1–10), recycled across biomes (Brandon+Lyra,
  Aug 23). Drop into `Sparkforge/Audio/BGM/` per its README — zero code.
- v2.0.1 verdict: on approval, flip live-version records + Notion
  chain-of-events.
- v2.0.5 shelf: Mote sprite (art-blocked), bestiary shipped, Skybeam/
  Thornwall folded into the abilities track.

## Cross-track notes

*(append here when a track touches another's seams)*

- Geometry → Abilities: new placement effects (Lightning Sentry coils,
  Glacial Spikes, black holes) MUST use `PlacementSampler`/`resolve` —
  the primitives exist as of `cd63d6c`.
- Abilities → Content: bestiary/codex ids and Boss Mode dials are shared;
  new-card ids never rename (persistence canon).
