# Sparkforge v1.8 — "Mirrorwound + The Codex" Sprint Kickoff

*Scoped July 12, 2026, from `v1.8-seeds.md` (Brandon's post-v1.7 device
notes) + kickoff decisions. Brandon's framing of the seeds: "road
detours with a couple extra miles, not a new destination." Pending the
Lyra creative pass (brief asks in Waiting-on). DoD gates between units
stay absolute; the cut-first list exists from day one.*

**Thesis:** The arena keeps growing (Mirrorwound, arena 4) and the game
learns to *remember* — a Codex Suite (synergies, cards, bestiary) turns
a run's discoveries into a permanent collection. The endgame loop earns
a new reward (Forge XP Coins), and the Remove Ads pitch finally explains
itself. v2.0's Mote gets its #1 prerequisite a version early.

## Decided — do not relitigate

- **Pairing: Mirrorwound (arena) + Codex Suite (system).** The codex IS
  a retention system — strong pairing. **Content>systems canon holds:**
  if the month strains, **the arena ships and the Codex Suite slips
  whole** (cut-first, Unit 5+). E1/B/C/D/E are the protected core and
  ship regardless.
- **E1 price fix = Unit 0.** The $2.99 label vs $3.99 charge is
  compliance-adjacent; first thing built. **Real fix: kill the
  hardcode** — pull the localized price string from the StoreKit product
  (IAPManager already fetches products). Never wrong in any storefront.
- **Card codex "discovered" = ever OFFERED** (revealed the moment a card
  appears in a selection, passed-over or not). Same semantics apply to
  synergies-seen. Rewards exploration; faster, livelier fill.
- **Forge XP Coins:** **flat value — the XP Boost (2x) ad does NOT double
  them** (fixed post-boss bonus lane, easy to reason about). **NO magnet
  pull** — walking to them is the point (health-orb canon). Ember-orange,
  large, spinning (xScale oscillation). Arena-wide scatter + despawn
  timer. Claims physics bit **`0x1 << 8`** → update Portfolio Registry
  ladder when built.
- **In-run "No Ads" affordance = upper-LEFT, above the buff badges.**
  Clear of the pause button — **the v1.7 pause hit-zone fix is not
  touched or shrunk.**
- **Bestiary is pulled forward from v2.0** (Brandon's call). **Reserve
  the hidden Mote `????` entry in the data model now** so the schema
  doesn't churn at v2.0 (`docs/mote-v2.0-handoff.md`).
- **Synergy unlock modal QUEUES, never stacks.** Multi-tier in one pick
  (Extra Pick chains) presents modals one after another.
- **Ashling shard protection = a scripted ~0.4–0.6s beat**, not an
  i-frame / invuln. Parent dies → shake/explode → THEN shards pop up,
  vulnerable once visible. The delay *is* the protection.

## Unit sequence

### Unit 0 — Remove Ads price, de-hardcoded (E1)
- Stop hardcoding "$2.99". Pull the localized price string from the
  StoreKit product via IAPManager; render whatever StoreKit returns.
- Graceful fallback if products haven't loaded (no naked/placeholder
  price shown).
**DoD:** title shows the true localized price on device; no hardcoded
price string remains; correct in a non-USD storefront (sandbox check).

### Unit 1 — Arena tuning & shard fix (B1 + B2)
- **B1 Arena 1 pacing +smidge:** dial up slightly (candidates: spawn
  interval floor, melee thinning chance, ranged spawn chance/time —
  `GameConfig` only, small moves). Post-boss swarm stays untouched.
- **B2 Ashling shard spawn protection:** `spawnAshlingShards` currently
  fires synchronously in `onEnemyKilled`; insert the scripted ~0.4–0.6s
  beat between parent-death VFX and shard physics existing.
**DoD:** Arena 1 no longer "stand still and machine-gun"; Ashling
parent's death AoE can't insta-kill the trio; shards vulnerable once
visible. Both validated on device.

### Unit 2 — Forge XP Coins (C)
- New boss-death pickup alongside the existing XP-orb shower (KEEP the
  shower — it works brilliantly). Boss also erupts large spinning
  ember-orange coins scattered arena-wide.
- Physics bit `0x1 << 8`; despawn timer (tune); no magnet; collected →
  `pendingForgeXP` / `addForgeXP` pipeline; flat (no XP Boost doubling).
**DoD:** coins scatter arena-wide on boss death, read as distinct from
blue magnet / green health / small XP orbs; collect into forge XP;
60fps held during the post-boss swarm.

### Unit 3 — Arena selector locked-state cleanup (D)
- "LOCKED" all-caps, prominent, larger than the requirement text below
  it; icon/text hierarchy tidied. Touches `refreshArenaSection`'s locked
  branch only.
**DoD:** locked arenas read clearly during a real unlock moment; no
other arena-selector regressions.

### Unit 4 — Remove Ads package: pitch & in-run (E2 + E3 + E4)
- **E2:** Remove Ads becomes a distinct box/rectangle, restyled. Final
  title-screen position settled in Unit 10's bottom-band exercise;
  interim placement away from menu/codex entry points (zero accidental
  taps).
- **E3:** value-prop modal on tap, BEFORE the purchase sheet — "Remove
  all ads — keep every reward. Just tap and you're rewarded." (revive,
  reroll, extra card, extra pick, blessing choice, XP boost all become
  free taps).
- **E4:** tiny logo-type "No Ads" icon in-run, **upper-LEFT above the
  buff badges**, out of the field of play.
**DoD:** value-prop shows before purchase; in-run icon legible and clear
of the pause target; Remove Ads owners see the free-tap behavior.

--- Codex Suite (system) ⚠️ cut-first if the month strains ---

### Unit 5 — Codex persistence foundations
- New lifetime `sf_` keys (never rename; live-player data canon):
  synergies-discovered, cards-offered, bestiary encountered/killed-per-
  family. (Current `appliedSynergies` is per-run only.)
- **Reserve the hidden Mote bestiary slot** in the data model (hidden
  until v2.0).
**DoD:** discoveries persist across app restarts; schema includes the
reserved Mote slot; no migration breaks existing saves.

### Unit 6 — Synergy unlock modal (A1)
- Replace the bottom floating line at 3x/5x/7x with a pause-step modal
  (card-selection behavior: game holds, tap to continue). Card language:
  tree-tinted plate, colored stroke, tag emoji, `brightColor()` title,
  white effect text. Slots where `showSynergyNotification` fires;
  `applySynergy` descriptions feed the copy.
- **Queues** on multi-tier / Extra Pick chains.
**DoD:** each tier earned presents its own modal in sequence; game holds
and resumes cleanly; reads as card-family.

### Unit 7 — Synergies codex page (A2)
- All 18 synergy tiers (6 trees × 3: Fire/Shock/Bleed/Guard/Void/Chill;
  Neutral is a tag but has no synergy). Discovered → revealed;
  undiscovered → tree-tinted card with "???". Access: pause menu + main
  menu. Functional copy already exists in `synergyDescription`.
**DoD:** every tier present; discovered/undiscovered states correct
against Unit 5 persistence.

### Unit 8 — Card codex page (A3)
- The 56-card pool. Discovered (ever OFFERED) → full render; undiscovered
  → "???" in tree colors.
**DoD:** all 56 present; "offered" reveal fires correctly; states match
persistence.

### Unit 9 — Bestiary page (A4)
- Enemy families to date: melee, ranged, Ashling, Braceguard, Relay Imp,
  Grounder, Static Halo, Circuit Wasp + 3 bosses (+ mini-boss). Plus the
  **reserved Mote `????` slot, hidden until v2.0.**
- Encountered → revealed; killed count per family.
**DoD:** every current family present with correct encountered/killed
states; Mote slot exists but does not render pre-v2.0.

### Unit 10 — Title bottom-band navigation
- Design the title's bottom band as ONE exercise: codex / bestiary /
  synergies entry points + the E2 Remove Ads box, arranged so nothing
  collides and Remove Ads gets zero accidental taps.
- If the Codex Suite slips, this unit still lands the E2 Remove Ads
  relocation on a coherent bottom band (protected).
**DoD:** all entry points reachable and unambiguous; Remove Ads visually
separated; no accidental-tap overlap.

--- Mirrorwound (arena 4) — PROTECTED content pillar ---
*(Units 11–14 gated on the Lyra brief — see Waiting-on. Palette, motif,
enemy family, boss, and cards all await the creative pass.)*

### Unit 11 — Mirrorwound arena
- ArenaConfig entry (palette/motif per Lyra); gate criteria (tune at
  build, extend the existing unlock ladder).
**DoD:** fourth arena selectable, visually distinct, gated correctly.

### Unit 12 — Mirrorwound enemies
- New enemy family per Lyra (introduction order per the established
  curriculum pattern).
**DoD:** staggered introduction; distinct pressure from prior arenas.

### Unit 13 — Mirrorwound boss
**DoD:** winnable/lethal on device; distinct from Titan / Warden /
Dynamo Choir.

### Unit 14 — Mirrorwound cards (Lyra brief)
- New card set; dual-tag rule as established (counts toward BOTH tags).
**DoD:** every card true; tag counts stay ≥7 everywhere.

## Ship pass
Tuning from playtests → version bump 1.8 → device pass → screenshots
(codex/bestiary are new store-shot candidates) → What's New + promo →
submit → post-ship Notion pass (Portfolio Registry: physics bit
`0x1 << 8` claimed; any new AdMob units).

## Waiting on
- **Lyra brief v1.8** (draft asks, from seeds §F):
  1. Mirrorwound confirmation + palette/motif
  2. Codex page art direction — three pages, ONE visual language
  3. Synergy modal copy for all 18 tiers (6 trees × 3) — elevate the
     existing functional strings, or ship as-is
  4. Bestiary entry copy per enemy family (+ reserved Mote slot)
  5. Forge-coin visual identity
  6. Arena-4 enemy family + boss + cards
- **Brandon:** GitHub force-push approval (still pending from v1.7).
- **Brandon (if any new ad placement emerges):** AdMob unit + registry.

## Banked / explicitly out of scope this sprint
(from seeds §F — not relitigated here): Forge Path management screen +
respec, stat-boost card vein, projectile-mod card vein
(lightning/frost-touched — check shatter-machinery overlap), cross-arena
mob mixing, mini-boss mashups, bosses-only mode, vertical card spikes,
Game Center Daily Trial, Suno BGM into the shipped scaffold, DEBUG-only
playtest handicaps (god mode / gate bypass).
