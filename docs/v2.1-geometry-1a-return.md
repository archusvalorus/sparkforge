# v2.1 Geometry Unit 1A — Return Packet

*Claude → Brandon + Lyra, Aug 2026. Per the reconciliation's §9 handoff
instruction: touched files, final data structures + insertion seams, proof
results per criterion, performance sample with device context, discovered
incompatibilities, and a 1B recommendation. Built on Brandon's "you have
the floor on the geometry" authorization; scope held to §5 Unit 1A only —
no route behavior, no projectile blocking, no LoS, no roster, no
presentation.*

## Touched files

| File | Change |
|---|---|
| `Config/ArenaGeometry.swift` **(new)** | The contract: `Footprint` (convex rounded-rect SDF), `ArenaGeometry` (footprints/route data/spawn zones/anchors + queries), `PlacementSampler`, and the authored `.splitworks` layout |
| `Utils/GeometryDebug.swift` **(new)** | §10.5 overlay + resolve/rejection counters; the two announced DEBUG seams |
| `Utils/VectorMath.swift` | `dot`, `rotated(by:)`, `lerp(to:t:)` |
| `Config/ArenaConfig.swift` | `geometryBuilder` field (`.open` default) + the **Splitworks shell** entry (NOT in `all`) + the transient shell override |
| `Config/GameConfig.swift` | `Wave.spawnDistance` frozen `static let` → computed (the recon's latent bug) |
| `Nodes/ArenaBossNode.swift` | `geometryFootprintRadius` (default = body-sized; explicit, separate from contact bodies) |
| `Nodes/MonumentBossNode.swift` | footprint radius 0 (a monument IS geometry) |
| `Nodes/EnemyNode.swift` | `geometryFootprintRadius` (visual × scale; override hook) |
| `Nodes/PaneStalkerNode.swift` | exempt while phased, solid while solid |
| `Nodes/HealthOrbNode / MagnetOrbNode / ForgeCoinNode.swift` | `randomArenaPosition()` → shared sampler |
| `Scenes/GameScene.swift` | player resolve after move + all pulls (Quench field, Gravemote, Weight of the Center); enemy post-chase resolve pass; run-start + Boss-Mode-swap safe anchor; loose-node clamp resolves geometry; Terra ghost / Singularity wells / Rift + Echo origins / panda destination / Erasure teleport / panda pin validity; Carrier body visual; overlay + HUD banner |

## Final data structures + insertion seams

Exactly the reconciliation §4 shape:

```
ArenaGeometry { blockedFootprints, routeNodes, routeEdges, spawnZones, safeAnchors }
Footprint { center, halfExtents, cornerRadius, rotation, label }
  contains(p, margin) · resolve(p, actorRadius) · intersectsSegment(a, b, travelRadius)
PlacementSampler.randomPoint(in:, minRadius:, maxRadius:, margin:)   // area-uniform, rejects, never nil
```

- **Enforcement = displacement**, monument precedent, no physics body.
  Physics bit «3 (`boundary`) left as-is (reconciliation: not work for its
  own sake).
- **`resolve` iterates** (≤4 gradient steps): a rounded-box SDF is exact
  outside but a lower bound inside, so a one-step push undershoots from
  deep interior. Found by the proof harness, fixed, re-proven idempotent.
- **`geometry` is a builder on ArenaConfig, materialized once per
  `setupArena`** — layouts size off the live arena radius, and a stored
  geometry inside a `static let` config recursed into its own
  `dispatch_once` (SIGTRAP at first launch; same class of bug as the
  frozen `spawnDistance`, fixed the same way).
- **Seams (all announced by the HUD banner while on):**
  `GeometryDebug.showOverlay` (draw footprint/margin/routes/gates/anchors)
  and `GeometryDebug.forceSplitworksShell` (play any selected arena AS the
  shell, via the transient Boss-Mode override — cleared on return to
  title, never persisted; the shell is not in `ArenaConfig.all`, so
  progression can't see it). Both `false` at HEAD; sim reinstalled cold.
- **Actor footprint radius** is explicit and separate from contact bodies
  (§7.2). Reads scale, unlike the physics body.

## Proof results (per §5 Unit 1A criteria)

| Criterion | Result | How |
|---|---|---|
| Player traverses both routes without clipping/sticking/entering the Carrier via movement or pulls | ✅ **math** / ⏳ **feel** | Resolve is applied after move + every pull site (Quench, Gravemote, Weight of the Center — the last one previously never re-ran ANY solid resolve). Proof harness: resolve from deep interior exits, is idempotent, near-edge stays local (<40pt, no teleport across). **Brandon's device pass owns the feel** (does the slide along the Carrier feel right, is the narrow route wide enough) |
| Small/medium/large existing enemies cannot remain embedded | ✅ | Post-chase pass every frame using per-actor footprint radius; harness: bigger actor resolves further out; PaneStalker exempt only while phased |
| No pickup / well / rooted object / persistent zone / random origin committed inside the footprint | ✅ | Health/magnet/coin samplers, Singularity wells, Rift + Echo origins, Terra ghost, panda destination + pin, Erasure teleport, loose-node clamp all route through the sampler or `resolve`. Harness: **0 of 2,000** sampled points inside (margin 20), rejections counted |
| Boss Mode enters at a valid safe anchor | ✅ | `swapArena` + `setupPlayer` use `anchor("player_spawn")` (falls back to origin for open arenas — verified in the sim: Spark enters at the south merge, Carrier ahead) |
| Spawn distance changes with arena radius | ✅ | `spawnDistance` computed; Star Anvil's hand-workaround now redundant (left in place, harmless) |
| Overlay shows footprint, margins, spawn zones, anchors, rejected samples, resolution events | ✅ (visual) / partial (events) | Screenshot-verified: footprint, margin band, route nodes+edges, 4 gate arcs, 2 anchors, banner. **Rejected-sample markers and per-event resolve markers are COUNTERS (`GeometryDebug.summary`), not yet drawn** — deferred to 1B where they matter (route decisions); noting the gap honestly |
| Worst-case swarm within perf target on the oldest device; else record context | ✅ **synthetic** / ⏳ **device** | See below |

**Open-arena regression check:** every query short-circuits on
`hasBlockedGeometry == false`; arenas 1–5 use `.open`. Build clean, zero
new warnings. Static launch + title + run start verified in sim on the
default (open) arena after the seams were colded.

## Performance sample

- **Synthetic (host):** Apple M5 Pro, `swiftc -O`: 300 enemies resolved +
  3 player resolves + 20 swept segment tests per frame = **0.003 ms/frame**
  (0.02% of a 16.67 ms budget). Even at 50× slower silicon this is <1% of
  a frame — the bounding-circle pre-reject does its job.
- **Simulator (iPhone 17 Pro sim, iOS 26, debug build):** Splitworks shell
  with overlay on, early run: 60.0 fps, 125–131 nodes. Not a swarm; the
  sim can't show headroom above 60.
- **Device floor: NOT MEASURED.** Per the reconciliation, no invented
  baseline: Brandon's device pass on the iPhone 15 (the studio's known
  device) at a late-run swarm is the real number. Recommend recording
  device / OS / build / enemy count / avg + low fps into this doc.

## Discovered incompatibilities / rulings needed

1. **None blocking.** Two engineering findings, both fixed in-unit:
   the `static let` recursion (geometry must be built, not stored, on a
   static config) and the resolve undershoot (iterate).
2. **Layout tuning is Brandon's:** the Carrier currently reads slightly
   larger than the lock's "~1/3 of usable length" target — a
   half-extents/corner tweak in ONE place (`ArenaGeometry.splitworks`).
   Feel it before touching it.
3. **Nice accident recorded:** Lightning Sentry T4 (card rework) places a
   coil at arena centre — inside the Carrier. The sampler/resolve now
   exists to catch it. First ability × geometry test case, for free.

## Brandon's 1A feel pass — PASSED (Aug 23)

"Overall it felt good." Verdicts recorded: **cramped stays** (forces
movement choices — matches the lock's "dense, not sprawling"); the slab
reads as placeholder (it IS the Carrier's shell body — final silhouette =
Unit 4 art); **more rendered clutter wanted** → maps to the lock's §9
environmental anchors, all NON-blocking, lands in Unit 4; arena expansion
considered and HELD (cramped is the feature). **Real-device swarm perf
number DEFERRED by Brandon's call** until the design philosophy + final
level design are pinned — remains an open criterion for Unit 5 closeout,
not a 1B blocker.

## Recommendation: 1B can proceed unchanged

The route data (nodes, edges, labels) is authored and drawn; the
`segmentBlocked` primitive that 1B's "is the direct segment blocked?"
decision needs is proven (including fast-step tunneling). 1B is behavior on
top of data that already exists: blocked-segment detection before `chase`,
choose/commit/arrive/re-decide/recover, congestion weighting only if route
collapse shows up, stuck/oscillation diagnostics — and it's where the
event-marker overlay gap above gets closed. No architecture change needed.
On Brandon's go.

---

# Unit 1B return — authored route graph + recovery (appended)

*Built on the 1A pass verdict, same session. Scope per reconciliation §5
Unit 1B; proof against its criteria.*

**Shape:** one steer pass (`routeSteerTarget`) ahead of the chase dispatch —
the §4 decision-point model verbatim: direct-clear → pursue directly
(open arenas always short-circuit); blocked → commit to a visible node
scoring `d(enemy,node) + d(node,goal)` with a backtrack penalty
(oscillation guard) and a 0.4s re-decide cooldown (thrash guard); commit
holds until arrival (28pt), invalidation (displacement broke the line),
or the direct line clearing. No node visible → beeline+slide, counted.
All ten chase behaviors untouched — they chase whatever point the steer
hands them; multi-hop routes emerge from arrival→re-score (no path
search). Tunables in `GameConfig.Routing`. Ranged mobs inherit routing;
their LoS-aware FIRING stays Unit 2 scope (Ruling 4).

**Proof (Splitworks shell, live run, overlay + counters):**
- Enemies from the far side reach the player around either flank —
  level-10 run at 1:25 with continuous kills + contact damage taken.
- Counters after ~90s: **decide=84 · recover=0 · fallback=0 · resume=38**
  — zero frames with no visible node (the 6-node graph covers the yard),
  zero displacement recoveries needed, clean direct-pursuit resumes.
  No oscillation observed (backtrack penalty + cooldown).
- 60.0 fps at 314 nodes with overlay + 5Hz commitment-line drawing on.
- Diagnostics: per-enemy commitment lines (5Hz, DEBUG+overlay only) +
  a 10s `[GEO]` counter dump; the 1A "events not drawn" gap is closed
  for routes (drawn) — rejected-sample markers remain counter-only.
- Deterministic + tunable: three constants, greedy scoring, no black box.

**Also in-unit:** AudioManager engine-start backoff (a wedged CoreAudio
daemon in the sim aborted inside `AVAudioEngine.start` — one retry per 5s
now; device risk was already negligible, retry-storms now impossible).

**Recommendation:** Unit 2 can proceed — the roster's spatial verbs
(Spurhound flank = this steering + emergence tells; Linekeeper anchor =
segment tests; Ramplate = charge + wall-termination precedent) all have
their primitives proven. Projectile blocking + targeting LoS are Unit 2's
two shared-system prerequisites, per the reconciliation.

---

# Unit 2a — Travel & vision policies (return, Sep 2026)

*The two shared-system prerequisites the reconciliation §5 puts ahead of
the Arena 6 roster, plus Ruling 4. Built; Claude's live proof was PARTIAL
(runs kept dying to laptop sleep). **GATE PASSED — Brandon's device run, Sep 8
2026: Splitworks shell past 0:45, shooters routing + holding fire behind the
Carrier, shots dying on the iron from both sides — "looking solid."** Files:
`GameScene.swift`, `RangedEnemyNode.swift`, `GeometryDebug.swift`,
`GameConfig.swift` (`Geometry` enum). Next = roster 2b Spurhound.*

## What landed

- **Swept projectile blocking** — player AND enemy projectiles: each frame,
  the segment from last position to current is tested against blocked
  geometry (`GameConfig.Geometry.projectileTravelRadius = 3`). Blocked →
  resolved to the impact point, muted ash-gray impact ring, removed.
  Gravity Well still spawns where the shot died (canon: wells form where
  shots die). Covers ~12 card effects via the single `updateProjectiles`
  funnel. Open arenas: one bool, zero cost.
- **Auto-aim line of sight** (`findNearestTargetPosition`): occluded
  enemies/boss are skipped; if nothing is visible the result is nil =
  HOLD FIRE (the clean fallback — firing into stone was the failure).
  Lasso priority stays first; if the lassoed prey is occluded it falls
  through to nearest-visible.
- **Summon-brain line of sight** (`findPriorityTarget`): the shared
  targeting brain (turrets, flowers, lion, kaiju, tree launches, Iron
  Maiden) tests visibility from ITS OWN origin. Cheap-skip before the
  segment test: only candidates that could beat the current best get
  tested.
- **Ruling 4 — legacy shooters seek line of sight**:
  `rangedChase(canFire:)` (default true — every legacy call site + open
  arena unchanged). While occluded, a shooter never stops-and-fires; it
  keeps approaching, and since the route steer hands it waypoints,
  "approaching" IS relocating around cover.
- Counters: `projectileBlocksPlayer/Enemy`, `losSuppressedTargets`,
  `rangedHeldFire` in the `[GEO]` dump.

## Proof

| Criterion | Result |
|---|---|
| Player shots stop at the Carrier | ✅ live — `blockP=3` in a 20s shell run |
| Auto-aim declines occluded targets, holds fire cleanly | ✅ live — `losSkip=105` in 20s; no shots into stone observed |
| Enemy shots stop at the Carrier | ✅ Brandon's device gate (Sep 8): "shots dying on the iron from both sides." Claude's 2b sim run: `blockE=0` alongside `heldFire=158` — shooters denied a line simply don't shoot it (the hold-fire path fires first). |
| Shooters keep moving while occluded | ✅ live — `heldFire=158` in the 10s after the 45s ranged spawn (2b sim run); Brandon: shooters "walk the route to find you." |
| Routing unaffected | ✅ `decide=29 recover=0 fallback=0 resume=12` |
| Open arenas unchanged | ✅ every policy short-circuits on `hasBlockedGeometry == false` |
| Build | ✅ clean, zero new warnings, combined tree with the abilities track's uncommitted work |

## Notes for the roster (2b–2d)

- Linekeeper's "respects direct-fire collision" and "relocates if denied
  a valid shot" are now FREE — it inherits Ruling 4 + projectile blocking
  by being a RangedEnemyNode.
- Spurhound's "select a route at a decision point" is 1B's
  `routeSteerTarget`; the flank preference is a scoring tweak.
- Ramplate needs the explicit `geometryFootprintOverride` (1A) for
  route-clearance; "cannot pivot during a committed charge" = the boss
  charge-termination precedent.


# Unit 2b — Spurhound (return, Sep 8 2026)

*Roster #1 of the Splitworks, per the reconciliation's "one enemy at a
time." Spatial verb: GO AROUND. Built on 1B + 2a. **GATE PASSED — Brandon's
device feel pass, Sep 15 2026.** Files: `SpurhoundNode.swift` (new),
`EnemyNode.swift`, `GameScene.swift`, `GameConfig.swift` (`Spurhound`),
`GeometryDebug.swift`.*

## What was built

- **`SpurhoundNode`** — four phases: **hunt** (follows the 1B steer target;
  while `isOccludedFromGoal` the sprint ramps to 2.3× base over 0.9s, and
  decays over 0.5s after re-emerging so it comes AROUND the corner fast
  instead of braking at it) → **tell** (in sight, within 210pt, off
  cooldown: exhaust flares wide + bright, hull crouches, lunge line
  committed at tell START — the Star Needle grammar, punish late movement
  not random movement) → **lunge** (600pt/s for 0.38s ≈ 228pt, fixed
  direction, no re-aim) → **recover**. Three lunge outcomes: **hit** (short
  0.6s recovery), **miss** (1.4s, punishable), **clang** (ran into the
  Carrier: bounce, ceramic-rim flash, 1.4s, punishable). Punishable =
  1.5× incoming damage, implemented in a `takeDamage` override so card
  marks on `vulnerabilityMultiplier` (Apex T4) are never stomped.
- **Flank preference** = the promised scoring tweak. `EnemyNode.
  routeNodeBias(_:goal:occupancy:)` (default 0 → every pre-2b enemy routes
  bit-identically to 1B). Spurhound: nodes on its own side of the player
  cost up to +140, far-side nodes earn up to −140, plus +60 per hound
  already committed to a node (the lock's "subject to crowding"). The
  scene rebuilds a per-frame `routeOccupancy` map (one O(n) pass, skipped
  on open arenas).
- **Two published facts** the scene already computed: `isOccludedFromGoal`
  (set in the steer pass — no second geometry query) and
  `geometryDisplacedThisFrame` (set by the resolve pass — "you hit the
  Carrier"). `didStrikePlayer()` fires from the contact handler BEFORE
  the i-frame guards, because the CONTACT is the outcome.
- **Splitworks spawn table** (`spawnSplitworksEnemy`, arena id 5): basic
  bodies from 0s, Spurhounds at 15s (35% of spawns, Beat 2 "remember the
  flank"), legacy shooters at 45s as the Linekeeper placeholder (2c),
  melee thinning inherited. Spawns come from the **authored gate zones**
  (`splitworksSpawnPoint` — random zone, random angle in its arc, resolved
  out of any footprint) — the lock's spawn zones start paying rent.
- **Visual grammar** (procedural, lock §5.1): low forward-canted wedge in
  charcoal iron with an oxidized-teal rim, pale-ceramic rail-skate spurs at
  the rear corners, kiln-orange exhaust plume that brightens with the
  sprint and flares on the tell. Chassis rotates to face travel.
- Counters: `hound[lunge/hit/miss/clang]` in the `[GEO]` dump.
- Dials: `GameConfig.Spurhound` (speeds, ramp, range, tell/lunge/recover
  timings, punish multiplier, cooldown, flank/crowd weights, spawn table).

## Proof (Claude — static sim run, iPhone 17 Pro, Spark parked at spawn)

| Criterion | Result |
|---|---|
| Hounds route around the Carrier and pick a side | ✅ `decide=82 recover=0 fallback=0 resume=44` over 50s with hounds in the mix; no fallbacks |
| Emergence tell → committed lunge | ✅ `lunge=3` by 0:40 |
| Wall clang is detected and punished | ✅ `clang=1` — a lunge that grazed the corner bounced and went punishable |
| Miss / hit accounting | ⏳ `hit=0 miss=0` — Spark stood still and auto-fire deleted 1-HP hounds mid-lunge (a death is its own outcome, not counted). Real accounting needs a moving player: Brandon's gate. |
| Pre-2b enemies unaffected | ✅ bias hook returns 0 for every other class; routing counters in line with 1B/2a runs |
| Open arenas unchanged | ✅ occupancy pass and bias only run when route nodes exist |
| Spawn zones honored | ✅ `rejects=0`; every spawn visibly from a gate arc |
| Build | ✅ clean, zero new warnings; 60fps at 323 nodes |

## Brandon's gate — what to feel for

1. **Does a hound going behind the Carrier feel like a threat you have to
   remember?** Watch the exhaust brighten as it disappears; it should come
   out the OTHER side faster than it went in.
2. **Is the tell fair?** 0.5s of flare + crouch, then a straight 228pt
   lunge. You should be able to sidestep on reaction; a late dodge should
   get clipped.
3. **Punish window** — after a miss or clang the hull dulls to overheated
   brown for 1.4s and takes 1.5×. Is that long enough to feel earned,
   short enough to stay dangerous?
4. **Pack behavior** — do two or three hounds split across both routes, or
   conga-line one side? (`flankBias` / `crowdPenalty` are the dials.)
5. Anything that feels like the Star Needle in a different coat is a
   tuning note, not a design note — the differentiator is the corner.

## Deferred (by design)

- Sound (acceleration whine, lock §9) → Unit 4 with the rest of the
  arena's audio. Bestiary entry + copy → Unit 4.
- The Linekeeper placeholder is the legacy shooter; 2c replaces it.

# Unit 2c — Linekeeper (return, Sep 15 2026)

*Roster #2 of the Splitworks. Spatial verb: HOLD A LINE. Built on 2a's
projectile blocking + the 1B steer facts. **GATE PASSED — Brandon's device
feel pass, Sep 15 2026: "ZOMG those enemies are amazing."** Files:
`LinekeeperNode.swift` (new), `EnemyProjectileNode.swift` (elongated bolt
option), `EnemyNode.swift` (`goalPosition`), `GameScene.swift`
(`findFiringAnchor`, spawn table, `spawnEnemyProjectile` params),
`GameConfig.swift` (`Linekeeper`), `GeometryDebug.swift` (counters).*

## What was built

- **`LinekeeperNode`** — three phases. **seek**: asks the scene for a
  VALID firing anchor (`findFiringAnchor`: at standoff 240pt from Spark,
  candidates fanned ±8 steps of 30° from the keeper's current bearing;
  rejected if inside geometry, if the bolt line to Spark is blocked, or if
  the walk from the keeper to the anchor is blocked; open arenas: first
  candidate wins), walks there directly; no anchor → follows the routed
  steer target and retries every 0.5s. Planted with a clear line → **aim**
  (0.7s grounded stance: legs spread, mast settles, the seam and a narrow
  violet line brighten along a direction LOCKED at aim start; a line that
  gets covered mid-aim is abandoned) → fires a fast narrow **bolt** (340
  pt/s, elongated capsule, `0xC58BFF` — purple family, canon) → **cooldown**
  2.4s → seek again (re-aims immediately if the anchor still holds).
  **Denied** a clear shot for 1.8s at its anchor → drops the anchor and
  relocates; Spark drifting past 1.7× standoff also re-anchors.
- The bolt inherits 2a's swept blocking — hard cover interrupts it exactly
  as the lock demands ("respects direct-fire collision with the Carrier").
- **`EnemyNode.goalPosition`**: the real goal each frame (the steer pass
  publishes it alongside `isOccludedFromGoal`), so anchor-seeking enemies
  aim at Spark even while `chase` is handed a route node.
- **Spawn table**: Linekeeper replaces the legacy shooter placeholder at
  45s (25% of spawns; Beat 3 "discover cover"). 1 HP, per the shooters
  canon (anti-turret tax).
- **Visual grammar** (lock §5.2): tall narrow mast on the core, ceramic
  surveying head with a purple slit, tripod legs in pale ceramic, a
  luminous purple barrel seam. "A boundary marker that developed hostile
  opinions."
- Counters: `keeper[anchor/shot/relocate]` in the `[GEO]` dump. Dials in
  `GameConfig.Linekeeper`.

## Proof (Claude — static sim run, iPhone 17 Pro, Spark parked at spawn)

| Criterion | Result |
|---|---|
| Linekeeper spawns from a gate zone at 45s, reads as a boundary marker | ✅ visible on screen (tripod, mast, ceramic head, purple slit) |
| Seeks a valid firing anchor | ✅ `anchor=1` within 5s of the first keeper's spawn; `rejects=0` |
| Telegraph → bolt; bolt dies on the Carrier | ⏳ NOT observed — a parked Spark levels every ~5s in the shell and each modal pauses the clock; the run ended at 0:58 with `shot=0`. Bolt blocking is the SAME 2a segment test that Brandon's device gate already observed for legacy shooters (`blockE`); Brandon's device pass closes this row. |
| Relocates when denied a shot | ⏳ same — needs a moving player who breaks the line |
| Spurhound accounting now that Spark takes hits | ✅ `lunge=5 hit=3 miss=0` — the strike hook lands (2b's open row is closed) |
| Pre-2c enemies unaffected | ✅ hounds/bodies unchanged; `decide/resume` counters in line with prior runs |
| Open arenas unchanged | ✅ anchor finder short-circuits to the first candidate; `goalPosition` is a plain assignment |
| Build | ✅ clean, zero new warnings; 60fps at ~320 nodes |

## Brandon's gate — what to feel for

1. **Does the keeper feel like an anchor, not a chaser?** It should walk to
   a spot at range, plant (legs spread, mast settles), and hold it.
2. **The tell.** 0.7s of a brightening violet line and seam before the
   bolt. Sidestepping the line should feel earned; standing in it should
   hurt.
3. **Cover works, and cover costs.** Step behind the Carrier: the bolt
   should die on the iron, and after ~2s the keeper should give up the
   line and walk to a new anchor with a clear shot. Meanwhile the hounds
   keep coming — that's Beat 5's cross-pressure starting to exist.
4. **Standoff distance** (240pt) — too close to kill trivially, or too far
   to feel present? `GameConfig.Linekeeper.standoff`.
5. Observation for you: dev-arena runs still count toward lifetime runs
   and kills (pre-existing behavior for any arena; the shell grants no
   unlocks). Fine? Or should the seam suppress run stats?

## Deferred (by design)

- Aim tone (lock §9) → Unit 4 sound pass. Bestiary entry + copy → Unit 4.
- Beat-2/3 pacing (hounds 15s, keepers 45s) is a first pass; Unit 5
  closeout tunes the table with all three natives in.

# Unit 2d — Ramplate (return, Sep 15 2026)

*Roster #3 of the Splitworks. Spatial verb: OCCUPY A ROUTE. Built on 1A's
explicit footprint + the BossNode charge-termination precedent. **GATE
PASSED — Brandon's device feel pass, Sep 15 2026: "beautiful telegraphing,
enough time to respond, and just genuinely fun and hilarious to watch."**
Files: `RamplateNode.swift` (new), `GameScene.swift`
(spawn table + shove/passage closures), `GameConfig.swift` (`Ramplate`),
`GeometryDebug.swift` (counters).*

## What was built

- **`RamplateNode`** — four phases. **patrol**: walks to the NARROW
  passage (the route nodes whose label starts with `narrow`, supplied by
  the scene) and paces between its ends; arenas with no such nodes get a
  slow, patient pursuer instead. **brace** (0.8s, when Spark is within
  280pt, in sight, off cooldown): swings to face, lights the furnace
  seams, lays down the charge lane as a translucent kiln-orange rectangle
  the full charge length, crouches back onto the drive. **charge**: a
  fixed line at 520pt/s — no pivot — that ends on (a) a clean hit: Spark
  is SHOVED 110pt along the charge line and resolved out of geometry and
  the wall, short 1.0s recovery; (b) the Carrier: the resolve pass
  displaces the plate → `geometryDisplacedThisFrame` → stop; (c) the
  arena wall: clamp and stop (the BossNode precedent); (d) max distance
  (0.9 × radius) → miss. (b)/(c)/(d) → **recover** 1.6s, plate dulls to
  overheated brown, takes 1.5× damage (same `takeDamage` idiom as the
  Spurhound; card marks untouched). Charge cooldown 4s.
- **Explicit footprint**: `geometryFootprintOverride = 1.8 × visual` for
  route clearance and the Carrier stop (1A's contract; combat hitbox is
  untouched per reconciliation §4 — the plate visual overhangs the contact
  circle, so a graze that only clips the plate's edge is a MISS. Reads
  generous to the player; flag if it reads wrong).
- **Spawn table**: joins at 70s, 15% of spawns, **max 2 alive** so the
  narrow route is contested, never simply closed (Beat 4: "surrender the
  short route"). HP = body + 3 (the Anvilborn elite pattern), worth +3 XP.
- **Visual grammar** (lock §5.3): wide plated face ahead of the core in
  charcoal iron with a teal rim, three kiln-orange furnace seams that
  brighten during the brace, two battered ceramic-edged drive blocks
  behind. "A wall that became impatient."
- Counters: `plate[brace/shove/miss/wall]` in the `[GEO]` dump. Dials in
  `GameConfig.Ramplate`.

## Proof (Claude — static sim run, iPhone 17 Pro, Spark parked at spawn)

To reach the plate inside a parked Spark's lifetime, `firstSpawnTime` was
set to 8s FOR THE RUN ONLY and reverted to 70 before the build gate (the
committed value is 70; `git diff` shows no residue).

| Criterion | Result |
|---|---|
| Spawns from a gate zone, walks toward the passage | ✅ on screen at 0:11 heading for the east (narrow) side |
| Brace telegraph → committed charge | ✅ `brace=2` by 0:33 |
| Clean hit SHOVES Spark (not burst damage) | ✅ `shove=1` — Spark displaced and resolved; HP fell by ordinary contact damage only |
| Charge stops at the Carrier | ✅ `wall=1` — screenshot shows the plate parked against the Carrier's west face in recovery |
| Miss accounting | ⏳ `miss=0` — needs a moving player who sidesteps the lane |
| Vulnerable recovery | ✅ code-symmetric with the Spurhound's proven punish window (dull plate, 1.5×) |
| Cap of 2 alive | ✅ spawn guard; ≤2 observed |
| Build | ✅ clean, zero new warnings at the committed value; 60fps at ~230 nodes |

## Brandon's gate — what to feel for

1. **Does the narrow route feel OWNED?** Two plates pacing it should make
   the long way round feel like the smart call, not a punishment.
2. **The brace.** 0.8s of seams brightening + the orange lane. You should
   be able to step OUT of the lane on reaction; standing in it should get
   you shoved.
3. **The shove.** 110pt along the line, then you're resolved off the
   Carrier/wall. Does it feel like a displacement (the lock's intent) or
   like a hit? Contact damage still applies at the normal melee rate.
4. **Recovery.** After a miss or a Carrier stop it sits dull for 1.6s and
   takes 1.5×. Long enough to punish, short enough to fear?
5. **Cross-pressure (Beat 5).** With hounds + keepers + plates all in, does
   the yard still read? This is the first time the whole roster is live.

## Deferred (by design)

- Brace impact sound (lock §9) → Unit 4. Bestiary entry + copy → Unit 4.
- Roster pacing (15s / 45s / 70s, shares 35/25/15) is a first pass; Unit 5
  closeout tunes the table.

# Unit 3 — The Marchwarden (return, Sep 15 2026)

*Arena 6's boss. Thesis (lock §6): "The boss does not change the arena. It
demonstrates mastery over the arena that already exists." **GATE PASSED —
Brandon's device feel pass, Sep 15 2026: "that boss is hilarious… he's
holding an actual flag and that is gold."** Files: `MarchwardenNode.swift` (new), `GameScene.swift`
(`spawnMarchwarden`, dispatch `case 5`, contact hook,
`splitworksSpawnPoint(zone:)`), `GameConfig.swift` (`Marchwarden`),
`GeometryDebug.swift` (counters).*

## What was built

Three verbs, cycled, one resolving at a time (the fairness constraint):

- **Right of Way** — DECLARES the route on Spark's side of the yard
  (the authored lane whose nodes are nearest Spark's x): a high-contrast
  floor signal lights the whole lane for 1.3s while the warden runs
  (300pt/s) to the lane's near end and its seams brighten; then a
  fixed-line **charge** at 560pt/s to the far end. It cannot steer. The
  charge ends at the far end, the arena wall (clamp — BossNode precedent),
  or the **Carrier** (the scene's resolve pass pushes it back along the
  lane by most of a step → the charge is over). A body contact during the
  charge SHOVES Spark 150pt along the line (resolved off geometry/wall)
  plus normal boss contact damage. 1.5s visible recovery.
- **Standardfall** — 3 standards around where Spark stood when the verb
  began, an ordered arc 0.3s apart, each landing zone telegraphed 1.05s
  (ceramic ring + contracting orange inner ring), landing as a planted
  spike + burst. They CROSS the Carrier (the canonized sky-strike
  exemption) but every landing point is resolved onto valid ground, so the
  exemption never leaves bad state. 62pt radius, 18 damage, boss-class.
- **Muster Signal** — the banner flares, the gate arc OPPOSITE the warden
  lights at the rim for 0.9s, then a bounded packet enters there through
  the same authored spawn zones as everything else: 3 bodies + 1
  Spurhound. Skipped entirely when the yard already holds 40+ enemies.
- **The Column Advances** (< 35% HP, once): cadence ×0.6, Standardfall
  chains straight into Right of Way, the rim and HP bar go kiln-orange,
  and the Carrier's stroke ignites in sequence (scene-side, visual only —
  collision untouched, per the lock).
- Death → `registerArenaBossDefeat("marchwarden")` through the Unit 0
  chokepoint (the v2.0.1 lesson: wired on day one), XP + forge coins.
  Bestiary card and the Marchworn skin are Unit 4. **Not yet in
  `BossRegistry`** — Boss Mode loads a boss's arena by index and Arena 6
  isn't in `ArenaConfig.all` until Unit 4 registers it; adding the entry
  early would crash the gauntlet.
- **Visual grammar**: charcoal core with a teal rim and kiln-orange eyes,
  a plow face ahead with three furnace seams, a signal mast behind with an
  orange banner (the thing that flares on every verb), teal HP bar, name.
  Counters: `warden[charge/fall/muster]`. Dials: `GameConfig.Marchwarden`
  (HP 140 + 6/30s scaling, contact 36, XP 96).

## Proof (Claude — static sim run, iPhone 17 Pro, Spark parked at spawn)

The arena boss is gated on 100 kills AND a 2:00 floor; a parked Spark
reaches neither. For THIS RUN ONLY `bossSpawnKills` was 5 and the arena
time floor 10s (both reverted before the build gate; `grep TEMP` = 0).

| Criterion | Result |
|---|---|
| Spawns at `boss_anchor`, entrance + camera reveal, name/HP bar | ✅ on screen at 0:22 |
| Right of Way: declare → committed charge | ✅ `charge=2` |
| Standardfall: 3 telegraphed, resolved landings | ✅ `fall=3` per cast |
| Muster Signal: opposite gate lit, bounded packet | ✅ `muster=1`; the mustered hounds then registered `lunge=2` |
| One verb at a time; visible recovery | ✅ by construction (phase machine); observed cycling |
| Charge stops on the Carrier | ⏳ NOT observed — the declared lane runs beside the Carrier by design; a stalled charge needs Spark to stand where the lane meets iron |
| The Column Advances | ⏳ NOT observed — a parked Spark can't take 140 HP below 35%; code-symmetric with the enrage pattern (Faceted Lie) |
| Build | ✅ clean, zero new warnings at the committed values; 60fps |

## Brandon's gate — what to feel for

1. **Right of Way.** Does the lane READ before it's used — is 1.3s of
   floor signal enough to leave the lane, and does the charge feel like a
   train, not a bump? Standing in it should get you shoved, not deleted.
2. **Standardfall behind cover.** Duck behind the Carrier: the standards
   should still come down on your side. That's the whole point of the
   verb — cover is never permanent.
3. **Muster.** Watch the rim: does the lit gate tell you where they're
   coming from BEFORE they arrive? Three bodies + a hound: enough to
   change the engagement, not a flood?
4. **The Column Advances.** Under 35%: faster, chained, the Carrier
   ignites. Does the yard feel like it's moving again?
5. **Numbers.** 140 HP, contact 36, standards 18. It's the beefiest arena
   boss so far by design (Arena 6 > Arena 4's 90); tune on feel.

## Deferred (Unit 4)

Marchworn skin, bestiary entry + Lyra's copy, `BossRegistry` entry +
`ArenaConfig.all` registration (progression), muster call + brace sounds,
final art. Unit 5: tuning with the full roster, perf, closeout.
