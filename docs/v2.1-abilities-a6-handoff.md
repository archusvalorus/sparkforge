# v2.1 Abilities Track — thread handoff for A6 (written Sep 24, after A5)

*For the NEXT abilities thread. The fourth thread ran A5 (Guard) from recon to
push. This is deliberately short: `docs/v2.1-abilities-a5-handoff.md` still
holds the working conventions (the unit loop, harness and stub rules, the
sim-proof tricks) and stays valid wherever this doesn't override it.*

## Open the new thread with

> "Abilities track for v2.1, continuing. Read `docs/v2.1-abilities-a6-handoff.md`,
> then `docs/v2.1-abilities-a5-handoff.md` (conventions), `docs/v2.1-abilities-kickoff.md`
> and `docs/v2.1-ability-closure-table.md`. A5 is done; A6 (Void) is next.
> Recon first, then its decision batch as CL-70… One unit at a time, DoD gates."

**Sequence (Brandon, Sep 24):** orient → source recon → reconcile the pending
CL-4 / CL-9 / CL-10 / CL-14 plus the new CL-70+ questions → **STOP for
Brandon's rulings** → bounded implementation.

## Where things stand

| Unit | State |
|---|---|
| A0 … A4c | ✅ (earlier handoffs) |
| **A5 Guard** | ✅ **`b3957f3`, PUSHED Sep 24** (recon/decisions `441c601`). CL-49…CL-69 plus gate rulings Q1–Q5 (closure table §B4). The independent review of freeze 2 PASSED, then the device gate: "Terra + Guard is way more fun than it should be." Packet: `docs/v2.1-abilities-a5-return.md`. |
| **A6 Void** | ⏭ **NEXT** |
| A7 eligibility/atlas/codex audit · A9 art (Lyra) | queued |

- **Remote:** current through `b3957f3`, with nothing unpushed. Never push without Brandon's say-so.
- **Atlas:** v9 at `https://claude.ai/artifact/BWDqor2fbAai7d3GJxeKui`. Read it, then publish with `url`.
- **Harnesses:** **500 across NINE**: damage 53 · signature 22 · fire 53 · chill 43 · shock 32 · bleed 115 · hud 16 · redsmile 59 · guard 107.
- Leave `WorkspaceSettings.xcsettings` and `sparkforge-card-atlas.html` untracked.
- **Neutral redesign remains deferred to v2.2 or later, with no promised slot.** During A6/A7, only necessary compatibility fixes are allowed. Do not redesign Neutral as part of Void, eligibility cleanup or closeout.
- **Stat baseline is frozen for A6.** Void must be implemented and validated against the current stable stat baseline. The global stat-pick rebalance waits until after the ability pass (see Carried → Tuning).

## What changed in the loop during A5 (apply to A6)

1. **The review order (Brandon, Sep 24):**
   freeze → **independent Reviewer** → bounded corrective pass if needed → independent re-review → **Brandon's device gate**.
   - The 5-lens internal review is implementer self-review. It never substitutes for the independent review.
2. **Implementation-gate rulings.** After the build, Brandon may rule on the interpretations the packet raises (A5 had Q1–Q5).
   - Record them verbatim in the closure table.
   - Change ONLY what they require.
   - Re-run every gate.
3. **Freeze manifests.** Before any corrective edit:
   - Verify the tree against the last manifest.
   - Snapshot the frozen files.
   - Ship the corrective diff on its own, with a negative control (the pre-fix behaviour must fail the new checks).
   - Append each freeze's manifest to the packet (A5 has freeze 1 and freeze 2).
4. **Harnesses compile the REAL config.**
   - `tools/signature-draw-harness/extract-guard-config.sh` pulls `GameConfig.Guard` out of the app source for every harness, so Stubs no longer mirror Guard.
   - If A6 adds a config enum that PlayerStats or UpgradeManager reads (e.g. `GameConfig.Void`), **generalize that script to take the enum name** rather than hand-mirroring. The A5 test lens showed a mirror lets a wrong builder pass.
5. **Scene-wiring checks.** GameScene isn't compiled by the harnesses. A5's guard harness reads its source (the WR group) to prove the calls exist and run in order. Every pure rule needs a matching wiring check, or the rule can be un-wired with every check still green.
6. **Ask, don't assume, on loaded words.** A5's "piercing" turned out to mean "bypass flat enemy DEF only" (CL-61). A6's **"armor penetration" (Phase T2)** is the same kind of word.
   - The only enemy defences are the Boss Mode flat DEF dial and Braceguard's shield.
   - Erasure T2 "Void-Touched" already bypasses the shield.
   - Recon it, and put it to Brandon as a question.
7. **Sim proof.**
   - Copy `/private/tmp/claude-501/-Users-b-lappy-toppy-Developer-sparkforge/34d0b5a3-262a-440a-a3ec-bc95344f57c6/scratchpad/simproof-a5.sh` (modes `base|split|self`; TEMP code only in an rsync'd copy) into your own scratchpad.
   - It **stubs AudioManager in the TEMP copy.** The simulator's CoreAudio RPC wedges and aborts the app (memory: sim-proof-audio-wedge). Read the `.ips` before blaming the unit.
   - Always reinstall a clean build afterwards.

## A6 — Void: what the next thread needs

**Scope** (kickoff A6 row; spec `docs/v2.1-card-rework-spec.md` Void table, Q-V1…Q-V5):
- **`VoidWellNode` presets:** ONE parametric node, per the spec's consolidation note. It covers Gravity Well, Null Bloom (small), Dead Circuit (grows, then collapses at 3), the ×3 **Blackhole** (every 5th shot; absorbs HOSTILE projectiles only, Q-V4), and ×7 **Singularity** (stuck enemies decompose).
- **Warp Shot:** starts slow, accelerates; slower = more damage.
- **Phase → Anomaly (signature ×3):** 4 stacks. Normals die, elites take 20% max HP, bosses 3%. Elites and bosses get a 2s cooldown. T2 = "armor penetration" (ASK); T3 = 2 stacks per hit.
- **Riftline:** pierce with per-enemy falloff.
- **Mass Tax:** removed (Codex migration by retired id).
- **Void Horror** (new): 8% per eligible PRIMARY hit → FEAR, flee 0.5s. Bosses immune; 2s immunity afterwards. Fleeing must respect routes and geometry.
- **Shadow Edge** (new): every 7th PRIMARY FIRING EVENT, a 7× blade, 125% damage, pierces 3. Counters never count echoes, chains or extras.
- **Synergy rename:** ×5 Event Horizon → **Listlessness**. This also fixes the name clash with Erasure's "Event Horizon" countdown; change the codex and HUD strings together.
- **DoD:** shot 35 = well + blade; elite/boss Anomaly cooldown 2s; fear respects geometry.

**Still-PENDING closure rows to put to Brandon** (all PARAM, recommendations already drafted in closure table §B):
- **CL-4:** the returned-projectile modifier list.
- **CL-9:** Singularity decompose (6%/s normals; elites 2%/s capped at 40% per trap; bosses immune to the trap, 1%/s while overlapping).
- **CL-10:** well presets and the live-well cap (4).
- **CL-14:** the Warp Shot curve (40% → 100% speed over 0.6s; 150% → 100% damage).

**Likely new CL-70… questions** (verify each in recon):
- **The Void damage flag.** CL-37 built it in A4c with ONE consequence (the Braceguard bypass) and said "A6 attaches Phase T2 and Anomaly to the same flag later". Rule exactly what attaches.
- **Void eligibility.** Today NO Void card past Phase `requires` anything (A5 recon fact 15). Author `requires: [.voidUnlocked]` across the tree, Erasure included. Red Smile already needs both unlocks; Silver Skin is Guard-only (CL-50).
- **Red Smile and the new Void triggers.**
  - Red Smile swings are primary HITS, not shots (CL-39). So they should apply Anomaly and Void Horror (which "hit" per primary hit) but never advance Shadow Edge's or Blackhole's shot counters.
  - Confirm, and wire it by card TEXT (CL-39's rule).
- **Anomaly vs shared hittability.** Anomaly, fear and wells versus phased/vanished actors: A4c's rule is that new effects respect `isHittable`.
- **Boss percentage semantics require explicit reconciliation.**
  - Determine whether Anomaly's stated 3% boss hit and Singularity's stated boss overlap percentage are already final boss values, or inputs to a shared `BossClass` scaling lever.
  - Apply boss scaling exactly once. Do not double-scale an already boss-specific percentage.
  - Put the resolved interpretation into the CL-70+ decision batch before implementation.
  - Boss kills still route through the A4b boss-kill chokepoint (`onLethalHit`).
- **Fear.** It's a new enemy state; check its interplay with stun, snowman, flight (Repulse T3 airborne), and the kaiju.
- **Performance.** Live wells × absorbed projectiles × re-launches; the stress gate follows the A4c "true-rate first" stance.
- **Card-pool / migration accounting.** Mass Tax is retired while A6 introduces or replaces Void cards.
  - During recon, derive the exact expected card-pool delta.
  - Confirm the stable and retired IDs and the Codex migration behavior.
  - Establish the expected Atlas count after A6.
  - Do not assume the current 79-card count remains correct. The redsmile harness (RC5) and the guard harness (CA1) both assert 79 today and will need the ruled number.
- **Unstable Core** stays unchanged in the spec, but A5 found:
  - its burst kills are uncredited (a bare `takeDamage` → only the reward-only prune; logged for closeout);
  - its purple self-burst ring breaks purple = danger (A7).

  Ask whether either is A6-adjacent, or leave both where they are.

## Carried (don't lose)

- **Closeout audit:**
  - `AudioManager` on a wedged CoreAudio (a possible device crash vector after audio interruptions; do NOT repair inside a unit, per Brandon, Sep 24).
  - Unstable Core's uncredited burst kills.
  - The orb-indicator restart bug.
  - The B1 stale-index DoT question.
  - The `drawBonusCard` eligibility gaps.
  - The Boss Mode DEF dial below 1.0.
- **Tuning (closeout, after the v2.1 ability pass):**
  - **Global stat-pick rebalance, deferred until after the ability pass:**
    - DEF pick → +1
    - ATK pick → +3
    - HP pick → +5
  - These values were identified after A5 device validation, because the reworked systems materially increase DEF's value.
  - **Do not perform this rebalance during A6.** Void must be implemented and validated against the current stable stat baseline.
- **A7:**
  - The A4c meter scene-boundary test hardening.
  - **A5 test hardening:** Guard regression coverage should cross the production scene-classification boundary for Repulse pin eligibility, so a snowman wiring mutation (such as incorrectly passing `isSnowman: false`) fails the retained suite. A7/test-hardening only, not A6 scope.
  - Chain Lightning's hittability.
  - The sweep/gun hit-chain duplication.
  - The Hunter's copy.
  - The gun's boss path lacks `apexRegisterAttack`.
  - The visual/hitbox mismatch log.
  - Atlas `caps()` comments.
  - Everglow's 15s vs 20s copy, and its pulse missing the boss.
  - Grounder/Relay `fromBossClass`.
  - The stale "Skybeam T5 rework: BANKED" proposal in the atlas.
  - Boss-hazard reach using the unshrunk player radius.
  - The legacy `applyKnockback` has no path test.
  - Monuments aren't solid for enemies.
  - Unstable Core's purple ring.
  - Dormant runtime to delete.
- **Geometry track:** the short-segment `segmentBlocked` hole, the Marchwarden DEBUG trap, and the Boss Mode/registry entries.
- **Future:** deployable-placement infrastructure (Terra's placement → Tesla Coil → mines/traps). Not a unit yet.
